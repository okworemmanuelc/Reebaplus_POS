# Build Log

Condensed reference of durable build notes — invariants, gotchas, migration/ADR
references, and API/file contracts worth remembering going forward. Verification
narrative (test counts, `flutter analyze` results, "owner confirmed on emulator"),
reproduction blow-by-blow, and most branch/commit hashes have been trimmed; the
full history is preserved verbatim in `build_log_history.md` (and in git —
`git log -p BUILD_LOG.md`). Newest first.

New entries: add a compact bullet at the top of the current month, same format —
`- **YYYY-MM-DD** — **<short title>**: <the durable fact/invariant/gotcha>.`

---

## September 2026
- **2026-09-18** — **Expenses adopts the shared tabbed-sliver scaffold; and `AppDatabase.close()` blocks in `tearDown` for any stream-backed screen (Issue #256 / PRD #239)**: Expenses was the last screen hitting *both* PRD #239 failure modes at once — a red 26px band with no expenses (64px in the harness sweep) and, populated, the list squeezed to **1.8dp of its 1,137dp**, the silent mode that renders nothing and reports nothing. It was not on the PRD's original five because the defect sat in a plain `Column` (fixed `_buildHeaderArea` above `Expanded(TabBarView)`), not in a `NestedScrollView` body. **Fix**: `TabbedSliverScaffold` from #243 — header (Total Expenses + period selector, Monthly Budget card) becomes a `SliverToBoxAdapter` that scrolls away, the `TabBar` moves **out of `AppBar.bottom`** into the pinned bar, and both tabs become `SliverPadding`+`SliverList.list`. `tabBarExtent` must be `max(kMinInteractiveDimension, getRSize(72))` because these tabs stack an icon above the label (Flutter's taller `_kTextAndIconTabHeight`); the 48dp default clips them. Empty states become `SliverFillRemaining(hasScrollBody: false)`, which takes `max(remainingExtent, childIntrinsicHeight)` — that alone retires the 26px band, since the message now scrolls into reach instead of overflowing. `AppRefreshWrapper` moves **up** to wrap the whole scaffold once (it was previously cloned inside each tab's `ListView`), matching Inventory. **Two horizontal overflows that the 800x360 sweep could never see, because it never ran at 320dp wide**: the budget-bar caption row overflowed **135px** (caption + "Set budget" button, neither able to yield) and the expense card's status/date row overflowed **181px** (`_StatusBadge` + full `MMM d, y • h:mm a` stamp, both non-flexible beside one `Expanded`). Both fixed by making the text flex and ellipsize — **no tap target was shrunk**, per the PRD. Confirms the PRD's own note that 320x568 portrait is in scope and that a landscape-only sweep is not sufficient evidence. **The durable test trap, and the expensive one**: `AppDatabase.close()` waits for open Drift stream subscriptions to unwind, and a `tearDown` body runs **outside** the tester's fake-async zone, so those cancellations never get real async time and the close blocks until the **10-minute** per-test timeout. Expenses reads through long-lived `businessScopedStream` providers; **Customer Detail loads via one-shot futures, which is the only reason its suite closes cleanly in `tearDown`** — so this is latent for every future stream-backed screen test, not an Expenses quirk. The fix is to close inside `runAsync`: `await disposeScreen(tester); await tester.runAsync(() => env.dispose());`. Symptom to recognise: measurements print, *then* the test times out, with `flutter_tester` **idle at 0% CPU** (blocked, not slow) and a `dart:isolate _RawReceivePort._handleMessage` stack. **Separate, equally costly**: `setupScreenTestEnvironment()` must be called from `setUp()`, never inside a `testWidgets` body — the harness docstring says so, and ignoring it blocks Drift's real SQLite I/O forever in exactly the same silent way. Two throwaway diagnostics and a `git worktree` baseline run were all lost to that before the control experiment (running the merged Customer Detail suite, which passed 10/10 in 4s) proved the environment innocent. **Finder trap**: "Expenses" is both a tab label and the app bar title, so `find.text('Expenses').last` taps the heading and the tab never switches — the scroll-retention assertion then reads a fresh `0.0` position and looks like a `PageStorageKey` failure that is not one. Use `find.widgetWithText(Tab, 'Expenses')`. New `test/expenses/expenses_viewport_test.dart` (11 tests): 4 viewports × populated/empty on the two mandatory assertions, plus per-tab scroll retention, sideways swipe, and the period selector surviving 800x360. Measured after the fix: 320x568 shows **2 complete hit-testable rows, 0 overflow**; 800x360 shows 0 rows *at rest* by design — the harness documents that no structure can seat a row there without hiding a control or shrinking a tap target, and what the fix guarantees is one scrollable surface where the row exists at full height and one scroll reaches it (before, there was nothing to scroll to at 1.8dp).
- **2026-09-18** — **bottom navigation bar slides away sideways during vertical scroll, returning on reverse scroll, tab switch, and rotation upright (Issue #258 / PRD #239)**: On landscape phone viewports (`orientation == Orientation.landscape && !context.isDesktop`), scrolling downward into content hides the 56dp bottom navigation bar via a 200ms `AnimationController` curved transition, reclaiming vertical viewport for the screen body without leaving a blank gap. **Structure (`lib/shared/widgets/main_layout.dart`)**: `_MainLayoutState` implements `TickerProviderStateMixin` with `_bottomBarController` (200ms) and `_bottomBarAnimation`. **NotificationListener**: `bodyWidget` is wrapped in `NotificationListener<ScrollMetricsNotification>` and `NotificationListener<ScrollNotification>` driving `_handleScrollNotification` with strict filters: `isMobileLandscape` (`orientation == Orientation.landscape && !context.isDesktop`), `Axis.vertical`, source navigator (`_nav.isActiveTabNavigator(Navigator.maybeOf(notificationContext))` so offstage tabs or unrelated navigators never drive or restore the bar), and root route (`ModalRoute.of(notificationContext)?.isFirst != false`). Pushed-route notifications (modals, dialogs, sub-routes) are ignored before any bar restoration or pixels evaluation. Hiding triggers on `UserScrollNotification(direction == ScrollDirection.reverse)` or positive scroll delta; showing triggers on forward scroll, returning to the top (`pixels <= 0`), switching tabs, unpopping detail routes, or when content shrinks so `maxScrollExtent <= 0` via `ScrollMetricsNotification`. Upright (`Orientation.portrait`) and desktop (`context.isDesktop`) viewports unconditionally clamp the controller value to 1.0 (bar stays visible). **Scaffold bottomNavigationBar resizing**: Wrapped `BottomNavigationBar` in `AnimatedBuilder` with `ClipRect` and `Align(alignment: Alignment.topCenter, heightFactor: _bottomBarAnimation.value)`. As `heightFactor` transitions from 1.0 to 0.0, the Scaffold's body automatically expands downward to the bottom edge. **Grid overflow fix (`lib/features/pos/widgets/product_grid.dart`)**: Stock count `Text` wrapped in `Flexible(child: Text(..., overflow: TextOverflow.ellipsis))` preventing RenderFlex text overflow on compact card bottom rows. `flutter analyze` clean with 0 errors and 0 warnings.
- **2026-09-17** — **a transparent `Container` is not a transparent barrier: `ColoredBox` hit-tests itself, so every open `AppDropdown` froze the page behind it (found on #259's emulator check)**: the owner reported "the page stops scrolling when a dropdown is open". The cause was **not** POS: `AppDropdown`'s tap-outside-to-dismiss barrier was a full-screen `Container(color: Colors.transparent)`, and **a `Container` with a colour builds a `ColoredBox`, which hit-tests itself even when the colour is fully transparent** — so the barrier consumed the drag and `behavior: HitTestBehavior.translucent` never got the chance to pass it through. `SizedBox.expand()` paints nothing and hit-tests nothing, which is what makes a translucent barrier actually translucent. Measured: scroll offset after a 120dp drag went **0.0dp → 100dp** with that one-line change. **This was latent app-wide across 58 dropdowns in 31 files and invisible only because the screens holding them did not scroll** — POS becoming one scrolling surface (#259) is what surfaced it. **Second half of the fix, and the reason not to stop at the barrier**: the menu follows its field via `CompositedTransformFollower`, so it does not drift — but a field inside a **lazily-built sliver unmounts** once it scrolls past the cache extent, and `showWhenUnlinked: false` then hides the menu while leaving `_isOpen` true and the barrier live: **an invisible full-screen surface eating every tap**. So `_openDropdown` now also closes on scroll. **The seam that reaches it**: scroll notifications travel to a scrollable's *ancestors* and the field is its *descendant*, so a `NotificationListener` around the dropdown would never see them — it listens to `Scrollable.maybeOf(context)?.position` directly (detached in both `_closeDropdown` and `dispose`). Net behaviour: one gesture both dismisses the menu and scrolls the page. `test/widgets/app_dropdown_scroll_test.dart` pins all four behaviours **in one file on purpose, because two of them are in tension** — the page must scroll while open, a scroll must dismiss, **a tap outside must still dismiss**, and picking must still report the value; "fixing" the scroll by deleting the barrier would silently cost tap-to-dismiss. Mutation-checked: dropping the position listener reddens exactly the dismiss-on-scroll case and nothing else. **Test-surface trap**: `tester.pumpWidget`'s default surface is **800x600**, so a barrier tap at `y: 700` lands off-screen and looks like a broken barrier — the first draft of this test failed for that reason, not a code fault.
- **2026-09-17** — **POS becomes one scrolling surface; a `Scaffold.appBar` is fixed chrome and cannot scroll away (Issue #259 / PRD #239)**: POS's product grid measured **0.0dp of 463dp** at 800x360 with a populated catalogue — the worst case in the whole PRD, and **silent**: a grid handed zero height renders nothing and **throws nothing**, so a cashier saw a screen that looked fine and could not start a sale. The old suite passed it for months because it only asserted overflow plus a `> 0.0dp` grid rect. **Fix**: `_buildPos` is a single `CustomScrollView` (`kPosScrollSurfaceKey`) — top bar sliver, tier/manufacturer/quick-sale row as `SliverToBoxAdapter` (scrolls away), search + chips frozen, grid as slivers. **`ProductGrid` now returns a sliver, not a box** (`SliverPadding`+`SliverGrid`/`SliverList`, `SliverFillRemaining(hasScrollBody: false)` when empty); it is POS-only, so there was no second caller to migrate. **The two primitives that made this cheap**: `PinnedHeaderSliver` (Flutter 3.24+) and `SliverFloatingHeader` (**3.27+**, the binding floor; this repo is on 3.44) **size themselves to their child**, so unlike a hand-written `SliverPersistentHeaderDelegate` there is **no declared extent to keep in step with the responsive scale** — which matters because the frozen band holds an `AppInput` that is 40dp short-viewport / 53dp comfortable and drifts again under `textScaler`. No `NestedScrollView`, so none of #243's overlap-absorber wiring applies here. **The blocker worth remembering**: a `Scaffold.appBar` is laid out before the body and cannot scroll, and `SharedScaffold` **substitutes a default `AppBar` when you pass `appBar: null`** — so suppressing it needs the new explicit `bodyOwnsAppBar: true` flag, not a null. The drawer is unaffected: it stays on the Scaffold and `DrawerHost` wraps the body, so a `MenuButton` inside the *sliver* app bar still opens it. **The one orientation branch is navigation, not layout** (PRD #239 amendment 3, same exception as #258): sideways the top bar is a `SliverFloatingHeader` and returns on reverse scroll, upright it is pinned and looks exactly as before. The scrolling structure itself is unconditional. **Design records corrected in the same PR, as #259 required**: ADR 0025 §5 said "structural re-flow (**rail layout**) is required" — it now says a re-flow is required without prescribing the rail, since its `textScaler` evidence showed *that* one was needed, not *which*; and `responsive-layout-plan.md` §4's open A-vs-B decision is settled for A with the original comparison kept as the record. **The accepted cost, now pinned by a test rather than left to be rediscovered**: frozen chrome grows with the system font size (exactly the rail's advantage), so a test asserts a complete card stays reachable sideways at **1.3 — the app's real ceiling, clamped in `main.dart:454`**; the prior suite's `textScaler` case and a first draft here both used larger scales the app can never actually render. **Budget assertions deleted on purpose**: the 165dp chrome ceiling carried its own warning ("Do not lower this budget to fit a control") after being raised from 160dp to admit a legitimate 48dp tap target, and the 320dp case asserted `> 0.0dp` while its comment recorded an "HONEST NUMBER: ~9.8dp" — a screen passing its own test while documented as unusable. `pos_home_screen_overflow_test.dart` keeps only the column arithmetic (now via the extracted `ProductGrid.columnsFor`); the two-assertion invariant lives in `pos_home_viewport_test.dart` (17 tests: 4 viewports × populated/empty, max text scale, every-control-present, freeze-at-top, scrolling with a dropdown open, search, category, add-to-cart, hold-to-edit, pull-to-refresh). **Test-mechanics traps**: a sliver grid builds **lazily**, so asserting "all 8 seeded tiles exist" asserts the cache extent, not behaviour — assert the *change* after filtering; the tier row **fully unmounts** once scrolled off, so a scroll-away assertion must accept "gone" as well as "moved up"; and the add-to-cart success toast leaves a pending timer that trips `!timersPending` unless it is pumped out before the tree comes down. `flutter analyze` clean; full suite **2171 passed / 271 skipped / 0 failures** (2026-09-17, before the date-dependent Customer Detail failures described below).
- **2026-09-17** — **Customer Detail screen migrates to shared `TabbedSliverScaffold` (Issue #245 / PRD #239)**: `CustomerDetailScreen` (`lib/features/customers/screens/customer_detail_screen.dart`) previously hand-rolled `DefaultTabController` and a private `NestedScrollView` with tabs returning fixed-height bands (`Column` with cards and filter controls) above `Expanded` or non-scrolling lists. At compact viewports (320x568 portrait and short landscape 800x360 / 915x412), the tall headers (`_buildHeader` + `_buildCreditCard` taking ~450dp) and unscaled buttons (`AmberButton` / `OutlinedButton`) caused severe horizontal RenderFlex overflows and vertical content starvation. **Fix**: adopted `TabbedSliverScaffold` from #243 / ADR 0027, owned `TabController` synced via `_syncTabController`, converted tab bodies to sliver lists (`_creditHistoryTabSlivers`, `_ordersTabSlivers`, `_cratesTabSlivers`) with `TabSliverView.storageKey`s, replaced fixed action buttons with compact `AppButton(size: AppButtonSize.small)`, clamped dropdown width to `context.getRSize(110)`, and wrapped balance text with `FittedBox` and `Flexible`. Empty states rendered via `SliverFillRemaining(hasScrollBody: false)`. Pinned `TabBar` floored at 48dp. **Tested via `test/customers/customer_detail_viewport_test.dart`** across 4 viewports (`phoneSe1Portrait`, `androidCompactLandscape`, `pixel7Landscape`, `pixel7Portrait`) in both populated and empty states, asserting zero overflow and at least one complete visible credit transaction row, plus sideways tab swiping and scroll offset retention across tab switches. `flutter analyze` clean with 0 errors and 0 warnings.
- **2026-09-17** — **overflow reports name their screen, and a harness sweep is not evidence that a screen is fine (Issue #241 / PRD #239)**: `OverflowRouteReporter.install()` (debug-only, in `main.dart` after `CrashReporter.install()`) tags every `overflowed by` report as `[overflow] route=<Screen> (tab: …, offstage) widget=<Widget> :: <summary>`. It walks the ancestors of the report's `DiagnosticsDebugCreator` element, not the top route, so a pre-warmed offstage tab is blamed correctly. **An overflow report fires at paint time — a zero-width box never paints, so never reports.** Sweep traps, all verified: (1) `find.byType(..., skipOffstage: true)` treats slivers scrolled below a viewport as offstage, and a starved `NestedScrollView` tab body sits exactly there at rest, so measure with `skipOffstage: false`; (2) the live `firstRunSurfaceStateProvider` graph holds a Drift stream that hangs `db.close()` in tearDown, so override it per data state; (3) one-shot DAO futures need `tester.runAsync` gaps, or detail screens are measured on their spinner. **The 800x360 harness sweep missed four defects the emulator walk caught** (empty Cart 15px, Supplier Detail 1.2px, Business Reports cards taller than the screen, Appearance portrait-only), so emulator verification stays mandatory per slice. Results and slices #255–#261 are posted on #239.
- **2026-09-17** — **a scroll can end inside layout, so `AppRefreshWrapper` refreshes only on a finger release and never `setState`s mid-frame**: closing the supplier form (the keyboard hides) resizes Inventory's `NestedScrollView`, whose `_NestedInnerBallisticScrollActivity.applyNewDimensions` ends the scroll **inside `RenderViewport.performLayout`**. The wrapper took that `ScrollEndNotification` as the pull's release and called `setState`, which threw `Build scheduled during frame`. Because `_onRefresh` is `async`, the throw surfaced as an unhandled Future error (`error_logs` row with no `context`, nothing in logcat under the IDE's structured errors) and skipped its `finally`, leaving `_refreshing` stuck on. **Rules**: only a `ScrollEndNotification` with `DragEndDetails` may fire a refresh, and settling the pull back during `SchedulerPhase.persistentCallbacks` goes through a post-frame callback. Regression test: `test/shared/widgets/app_refresh_wrapper_test.dart` (dispatches a scroll end from `performLayout`). **Debugging gotcha**: when the IDE pauses on an async framework assert, the full stack is recoverable from the synced `error_logs.stack_trace`.
- **2026-09-16** — **a `NestedScrollView` charges its body for the headers that scroll away, and a starved list reports nothing (Issue #243 / PRD #239)**: Inventory's stock list was **exactly 0.0dp tall** at 800x360 and 915x412 — not clipped, not short, *zero* — because the tab body was a non-scrolling `Column` with a fixed filter band over an `Expanded`, sitting inside a `NestedScrollView`. **`RenderSliverFillRemainingWithScrollable` lays the body out at `viewportMainAxisExtent - precedingScrollExtent`**, so the taller the scroll-away header the shorter the body, at rest, regardless of how much screen there is. Measured before: tab body **95.6dp** at 800x360 against a filter band of **92.6dp** — negative space, which no padding tweak can close, and a single `AppDropdown` already spends 73dp of it once its stacked label counts. **The red band is the harmless mode.** It only fires when the tab's content is fixed-height; with products in the catalogue the list is handed zero height, renders nothing, and throws **nothing at all** — so an overflow-only test passes a blank screen. That is why the suite asserts a **visible, hit-testable content row** as the second, load-bearing assertion. **Fix**: `TabbedSliverScaffold` (`lib/shared/widgets/tabbed_sliver_scaffold.dart`, **ADR 0027**) makes every tab its own `CustomScrollView` with its filter band as a sliver above its list, and owns the **`SliverOverlapAbsorber`/`SliverOverlapInjector` pairing** — which existed nowhere in this codebase and is the reason a tab otherwise paints its first rows *under* the pinned tab bar. Tab bodies roughly doubled (95.6 → **212.0dp** at 800x360; 297.3 → 420.0dp at 320x568), zero overflow anywhere. **A nested tab widget must now build a sliver, not a box** — `InventoryHistoryTab` returns a `SliverMainAxisGroup`; a box dropped into `TabSliverView.slivers` fails loudly at layout, which is correct. **Honest limit, recorded on purpose**: at 800x360 **no row is on screen at rest** and none can be — app bar + summary cards + tab bar + filter band exceed the whole 360dp viewport before a row is laid out, and buying the room would mean hiding a control or shrinking a tap target, both forbidden. What the fix guarantees is PRD #239's own wording: one scrollable surface, so the row exists at full height and **one scroll reaches it** (2–4 complete rows after a single 160dp drag at every viewport). That tension between #239's prose bullet and its own arithmetic is filed back on #239, not resolved here. **Test-mechanics traps worth keeping**: (1) `NestedScrollView` builds its **outer** viewport as a `CustomScrollView` too, so `find.byType(CustomScrollView).first` grabs the header scroller and every gesture lands on the wrong surface — scope through `TabSliverBody`; (2) `tester.fling(..., Offset(-400,0), 1000)` on a 412dp-wide pager carries it **two whole tabs** (pixels 0 → 824 = max), so a tab-swipe test must use a measured drag past the half-page snap point, not a fling; (3) duplicate local keys among sibling rows in a sliver list trip `SliverChildBuilderDelegate`'s salted-key assertion, so the row marker is **per-product** (`inventoryProductRowKeyPrefix`), never one shared const. **Shared harness**: `test/helpers/screen_harness.dart` now owns the in-memory Drift DB, Supabase init and Riverpod overrides — `pos_home_harness.dart` is a thin POS wrapper over it, and a screen costs ~10 lines to cover instead of ~380.
- **2026-09-16** — **one shared pinned tab-bar delegate replaces four duplicate classes (Issue #240 / PRD #239)**: `PinnedTabBarDelegate` (`lib/shared/widgets/pinned_tab_bar_delegate.dart`) consolidates the hand-rolled `_StickyTabBarDelegate`, `_SliverTabBarDelegate` (×3), and `_PinnedHeaderDelegate` across Inventory, Customer Detail, Supplier Detail, Driver Profile, and Orders. **Two defects eliminated across the entire app**: (1) **Height forcing** (`SizedBox(height: effectiveExtent, child: child)`) prevents the framework's `layoutExtent exceeds paintExtent` assertion under `NestedScrollView` when loosely-constrained children render even fractionally short of `maxExtent`; (2) **Tap-target floor** (`effectiveExtent = math.max(kMinInteractiveDimension, extent)`) stops responsive scale compression (`getRSize(60)` = 42dp at `spacingScale = 0.70` on 800×360 / 568×320 landscape phones) from dropping interactive tab bars below the 48dp WCAG 2.5.5 / Material accessibility floor. **A third defect was introduced and caught on review**: `shouldRebuild` first compared child **`Key`s**, and since no host passes a key every comparison was `null == null` — so the predicate always returned false and the pinned header painted its first child forever. On Orders that is everyday breakage (pick "This Week" and the dropdown still reads "Today"; type in search and the ✕ clear button never appears), because the delegate it replaced was `shouldRebuild => true`; on Inventory it strands the tab bar on a **disposed `TabController`** when the visible tab set changes. The premise behind the change was wrong too — `shouldRebuild` runs when a new *delegate instance* is installed, not per frame (instrumented: **4 header builds across a whole fling-and-settle**, and those come from the pinned header's own layout regardless). The predicate now compares `effectiveExtent` and the **child itself**. **Second review catch**: Supplier Detail and Driver Profile spend 8dp of the header on a bottom margin under the tab bar, so flooring the *header* at 48dp still left the TabBar at ~42dp on compact scales — `PinnedTabBarDelegate.withChrome` reserves the margin **on top of** the floor. Customer Detail has no margin and keeps the plain constructor. Verified via `test/widgets/pinned_tab_bar_delegate_test.dart` (13 tests) including an `androidCompactLandscape` (800×360) viewport pump and a host whose pinned header must follow a `setState`; mutation-checked in both directions. **Trap worth keeping**: a `Container`'s render object *includes its margin*, so keying the Container measured the header, not the tap target, and the chrome test stayed green under mutation until the key moved inside a `Padding`.
- **2026-09-16** — **a bare `Center` in a bounded slot is a full-width bar, not a pill**: every labelled `AppFAB` — "Add Store", "Add Customer", "Add Expense", "Add Supplier", "Save Count", "Record Activity", "Invite new staff", and `AppSpeedDialFab`'s single-action collapse — rendered **screen-wide with its left edge cut off at -16dp**. **`Center` is an `Align` with no `widthFactor`, and `RenderPositionedBox` shrink-wraps ONLY when its incoming `maxWidth` is infinite.** The `Scaffold` FAB slot passes **loose but BOUNDED** constraints (`0..screenWidth`), so the `Center` introduced by **cfd2165** (2026-08-11, the icon-only barcode-button redesign) expanded to fill and dragged the whole button out to the full screen width; `FloatingActionButtonLocation.endFloat` — the Scaffold default every affected screen relies on, since none of them pass a location — then right-aligned that over-wide box to `x = screenWidth - fabWidth - 16`, i.e. **-16**, which is why the left corner looked clipped while only the right kept a margin. **`BoxConstraints(minWidth: n)` leaves `maxWidth` INFINITE**, so the Container's 165dp floor never bounded anything: `enforce()` against the slot's constraints resolved it to `minWidth: 165, maxWidth: screenWidth`. Fix is `Center(widthFactor: 1.0, ...)` — shrink-wrap, and let the Container's `minWidth` set the 165dp floor. **The icon-only path escaped for a reason worth remembering**: its explicit `width: fabHeight` makes the constraints tight, so `constraints.constrain()` pins that `Center` to the square whatever `widthFactor` says. That asymmetry is how one commit broke **eight screens** while the variant it was written for looked perfect — and why it sat unnoticed for five weeks. `test/widgets/app_fab_sizing_test.dart` pins the seam at the `Scaffold` FAB slot (rendered rect only, never internals), asserting the min-width case at **`phoneMiniPortrait` (375x812) — the responsive baseline where `spacingScale == 1.0`** — so the spec's 165 is literal rather than recomputed through `rSize` (ADR 0025). Mutation-checked: reverting `widthFactor` turns the pill and min-width cases red and correctly leaves the icon-only case green. **Doc drift flagged, NOT fixed**: `context/ui-context.md` specifies `AppFAB` height 50px; the code uses `rSize(context, 48)`.
- **2026-09-15** — **the back button was dead on desktop, and `closeDrawer()` never closed anything**: `NavigationService.isDrawerOpen` ORs in `isDesktopNotifier`, so on desktop — where the drawer is a *permanent* sidebar — it is hard-**true**. `handleBackPress` step 1 read it, called `closeDrawer()` and returned, swallowing **every** back press: no nested pop, no tab fallback, no double-back exit. Worse, `closeDrawer()` routed to `mainScaffoldKey`, whose Scaffold **declares no drawer** (every screen builds its own), so it was a no-op everywhere — the store picker's `nav.closeDrawer()` never closed the drawer either. **The split**: keep `isDrawerOpen` as the *visibility* signal (the first-run rail needs desktop to count), add **`isModalDrawerOpen`** — excludes desktop — as the *dismissable* signal the back button reads. `DrawerPresence` now publishes a closer (`registerModalDrawerCloser`) that resolves `Scaffold.maybeOf(context)` lazily, so `closeDrawer()` asks the Scaffold that actually owns the drawer; `mainScaffoldKey` is only a fallback. Registered as an instance **tear-off** (Dart canonicalises those per object, so the `dispose` unregister matches). **`openDrawer()` had the identical wrong target** and could **not** reuse the closing seam — `DrawerPresence` lives inside `AppDrawer`, and Flutter does not build a drawer's child while it is shut, so at the moment you want to *open* one there is nothing mounted to ask. Added **`DrawerHost`**, mounted in the Scaffold's `body` at all 7 drawer-declaring sites. **The trap**: MainLayout keeps every visited tab mounted behind `Offstage`, so several hosts are alive at once and "most recently registered wins" opens a drawer on a screen the user cannot see — each host vets itself (`hasDrawer`, route `isCurrent`, `isActiveTabNavigator`) and `openDrawer()` takes the first of the newest-first walk that says yes. Nothing calls `openDrawer()` yet, which is exactly why it rotted unnoticed, so `test/tour/drawer_host_open_test.dart` + a third ban rule (every file declaring a `drawer:` must mount a `DrawerHost`) stand in for the missing caller; mutation-checked both ways. Also: `drawer_presence_ban_test`'s `(?:end)?drawer:` regex was case-**sensitive**, so Flutter's `endDrawer:` (capital D) escaped the ban entirely — now `caseSensitive: false`, and `\b` still keeps `AppDrawer:` out.
- **2026-09-13** — **the repo collapsed to one branch, and the July minimum-login fix finally landed**: `origin` went from 35 branches to 1. Of 26 candidates only **two** held work absent from main — everything else was **squash-merged** (GitHub says MERGED, but the branch tip is not an ancestor of main, which is why `git branch --no-merged` lists it and `git diff main...branch` still shows its whole delta; **neither is evidence of unmerged content — check the PR state, not the graph**) or dead by record (web-pos moved to the reebaplus-web repo, `wip/pre-split-snapshot` was split into branches that later merged). **The one that mattered**: `syncMinimumLogin` had pulled only `['profiles','businesses','stores','users']` since July while the fix sat unmerged on `fix/pos-and-login-stability`. It now pulls **`roles`, `role_permissions`, `user_businesses`** too (`tablesTotal` 4 → 7). Without them the permission set never resolves before `MainLayout` builds: login lands on the POS tab (`setCurrentUser` sets index 1), `MainLayout` hides POS for a role without `sales.make` and bounces to Home **only once `currentUserRoleProvider` + `rolePermissionsProvider` resolve** — on a fresh device-join those rows arrive only via the heavy background full pull, so if it stalls the user is stranded on a hidden tab with no bottom nav. **This is a precondition for the first-run rail (#231/#233)**, which assume `Gates.viewStores` / `stores.manage` resolve for a brand-new owner. **Merging a 234-commit-stale branch means main has usually already solved the same problem better, and the conflict is the only place you find out**: that branch's *other* half converted `deviceUserIdProvider`/`activeCustomerProvider` to plain `Provider`s to stop the disposed-notifier crash — superseded by `mirrorNotifier` (#153/PR #154), and taking it would have tripped the ban test. Its two-banner POS hint fix was superseded by the single combined banner (PR #148) and referenced `UiHintService.hintPosTapAdd`, **which no longer exists — it would not have compiled**. Its `needsStoreSelection` guard on the scan FAB was genuinely new and was kept. **`mirrorNotifier` returns a two-way-mirrored PROXY, not the service's own instance**, so the branch's `identical(...)` assertion was false by design and was rewritten to assert the mirror contract; non-disposal stays covered by the recompute / force-refresh tests. **#138 FCM push merged whole** — migration **0159 slotted into the gap main had left between 0158 and 0160**, so no renumber; ADR 0018 landed with no collision. Firebase stays pinned to **core 3.x / messaging 15.x** (resolved 3.15.2 / 15.2.10 against platform interface 6.0.3): the 4.x/16.x pairing is broken because `firebase_messaging` 16.4.2 still `extends FirebasePlugin` while `firebase_core_platform_interface` 7.1.0 renamed it. **Still unverified on device** — the emulator check (permission → `devices.fcm_token` populates → a real `console_broadcast` buzzes) is outstanding, and cloud 0159 has never been applied to prod under that number. `flutter analyze` clean; 1986 passed / 129 skipped / 0 failures.
- **2026-09-08** — **auth and onboarding landscape re-flow (Phase 1)**: Phase 1 of `docs/design/responsive-layout-plan.md` lands short viewport (`pixel7Landscape` 915×412, `androidCompactLandscape` 800×360) support across all pre-login auth and onboarding screens without removing any control or compressing tap targets below 48dp. **`AppButton` 48dp tap target compression closed**: `AppButton` previously scaled button height down to 37.8dp under `isShortViewport` (`context.getRSize(54)` at `spacingScale = 0.70`), breaching WCAG 2.5.5 / Material interactive minimums; normal (54) and large (60) sizes now clamp at `max(kMinInteractiveDimension, ...)`. **`BrandedAuthBackground` 480dp width cap vs Grid columns (`who_is_working_screen.dart`)**: deriving grid columns from raw `context.screenWidth` computed 5 columns in landscape (915dp), but `BrandedAuthBackground` constrains its inner container to `maxWidth: 480.0`, squeezing 5 columns into 480dp (~80dp per staff card); wrapped in `LayoutBuilder` so `effectiveColumns` and `cellWidth` derive from `constraints.maxWidth`, cleanly preserving 2 columns. **Pre-login auth bottom insets**: auth screens are the documented exception to the `deviceBottomPadding` rule — `LoginScreen` sets `resizeToAvoidBottomInset: false` and `auth_form_kit.dart` accounts for keyboard height directly via `MediaQuery.of(context).viewInsets.bottom` so inputs remain visible above the soft keyboard. **Two-column re-flow under `context.isShortViewport`**: `LoginScreen` (avatar, greeting, dots left; `PinKeypad` right) and `CreatePinScreen` (step header, title, dots left; `PinKeypad` right) re-flow into two columns instead of forcing the user to scroll a 320dp keypad in a 360–412dp viewport. **Unscrolled column migrations**: `BiometricSetupScreen` (previously threw RenderFlex overflow by 169 pixels in landscape), `SuccessDashboardEntryScreen`, and `ComingSoonScreen` migrated onto `AuthCenteredScroll`. **Timer leaks closed**: `SuccessDashboardEntryScreen` and `AccessGrantedScreen` previously left uncancelled timers on route pop/dispose (`A Timer is still pending even after the widget tree was disposed`); both now track explicit timers cancelled in `dispose()`. **Coverage**: 43 new widget tests across `test/auth/` pump all 7 modified and 7 verify-only screens at `pixel7Landscape`, `androidCompactLandscape`, and `pixel7Portrait`, with 0 overflows and 48dp floor assertions green.
- **2026-09-08** — **the store address is street + country, and the state/LGA pickers were the reason onboarding was hard**: audit of all four `stores`-writing surfaces (`STORE_CREATION_AUDIT.md`). **The one that actually blocked people**: `AutocompleteField.onChanged` fires on **every keystroke**, not only on picking a suggestion, and the CEO store step assigned `_storePhoneCtrl.text = dialCode` unconditionally — so touching Country after typing a phone number **erased it**, and blanked it to `''` while the country was still a partial string with no dial code, producing "Enter a valid phone number" on a field that looked filled in. Staff sign-up had the same line guarded with `if (dialCode.isNotEmpty)`; onboarding did not. Both now use `_applyDialCode`, which swaps only the prefix and no-ops until the typed country resolves. **Three more, all from the same two pickers**: outside Nigeria the LGA field was labelled "District (optional)" but validation rejected an empty value — a hard block for every non-Nigerian tenant; `isNigeria` is an exact whole-string match, so typing "Nigeria" letter by letter swapped State/LGA between Autocomplete and TextField repeatedly and destroyed their contents; and both accepted unconstrained free text anyway (`"Lag"`, `"lagos"` all pass), so they bought no data quality for the taps they cost. **The clincher — the mandatory LGA was thrown away**: the client fused four parts (`street, lga, state, country`) but `p_location` carried only three and the cloud rebuilt `concat_ws(', ', street, city, country)`. `stores` restores by PK upsert, so the **next pull overwrote the local row and the LGA vanished**. Address is now **`street, country`** everywhere (onboarding, staff sign-up, New/Edit Store), and `p_location` stops sending `city` — `concat_ws` skips the missing key, so cloud and client build the **identical** string and the divergence is gone by construction. **No cloud migration needed.** **Gotcha for the Edit sheet**: its parser read `locParts[1]` as the state, so a 2-part location would have put the **country into the State box** — it now reads segment 0 as street and the **last** segment as country, which handles legacy 3- and 4-part rows too (the middle segments are simply not re-collected and drop on the next save). `receiptStoreAddress` is untouched — it drops the trailing country segment, so `"14 Market Road, Nigeria"` still prints as `"14 Market Road"`. `kNigerianStates` / `kNigerianLgas` remain in the repo, now unreferenced. **Still open, flagged not fixed**: the New Store sheet writes via a raw `db.into(db.stores).insert` instead of `StoresDao.createStore` — skipping the business-scoping invariant, never setting `kind`, logging no activity — and both sheets' `stores.manage` write-boundary check `return`s **silently** instead of calling `showGateDenied`, so a denied save just does nothing.
- **2026-09-07** — **a `readOnly` + `onTap` field is a control, and no icon inspection can tell**: Phase 0 compacted `AppInput` to 40dp in short viewports and gated the 48dp tap-target floor on `_isInteractive`, which only ever looks at the prefix/suffix **icon**. A date-picker field (`readOnly: true` + `onTap`, calendar glyph purely decorative) is tapped anywhere on its **whole surface**, so its only tap target dropped to 40dp in landscape — under the Material / WCAG 2.5.5 48dp minimum. Three call sites, the complete set across all 30 `AppInput` files: `payments/widgets/record_supplier_activity.dart:436` and `:759`, `expenses/screens/add_expense_screen.dart:572`. **Two levers produce 40dp and a fix must close both**: the compact 40×40 icon constraints (covers a field that HAS an icon) *and* `isDense` + `vertical: 9.5` content padding (the only thing left when a field has none) — `AppInput` now derives `wholeSurfaceTap = readOnly && onTap != null`, suppresses the icon constraints, and sets `InputDecoration.constraints: minHeight 48`. **Only the second lever is actually load-bearing** — verified by probe: disabling `InputDecoration.constraints` turns the no-icon assertion red, but disabling the icon-constraint suppression leaves the whole suite green, because `minHeight: 48` already covers the icon case too. The first is kept for coherence (a 48dp field should not hold a 40dp icon box) and must not be mistaken for a second line of defence. A `readOnly` field **without** `onTap` is a display field, not a control, and still compacts to 40dp — so the ~8 display-only `readOnly` fields in `inventory_screen` / `product_detail_screen` are untouched. **`AppDropdown` was the same defect and is now fixed too, unconditionally** — its whole surface is a `GestureDetector` (`app_dropdown.dart:232`) and `onChanged` is required and non-nullable, so it is *always* a control and its floor is NOT gated on `isShortViewport` the way `AppInput`'s is. **It measured 43dp in portrait**, so this breach predates Phase 0 entirely; Phase 0 merely took it to 40dp in landscape. A `ConstrainedBox(minHeight: kMinInteractiveDimension)` sits **outside** the padded, keyed `Container` so the hit area, the painted surface and the size `_openDropdown` measures for the overlay all agree — and a caller-supplied `contentPadding` cannot breach it (without the floor, a zero-padding dropdown renders 15dp). **Two existing test assertions were pinning the defect** (43dp portrait, 40dp landscape) and were updated; `AppDropdown` also stops drifting under `textScaler 1.3` (was 44dp) because the floor now exceeds what scaled text produces. **The cost is real and was paid rather than dodged**: POS landscape chrome 154.2 → 162.2dp, grid ~98 → ~90dp, test budget 160 → 165dp, and on a 320dp-tall viewport the grid falls to ~9.8dp — no crash, but unusable, which is the single strongest argument that Phase 2's re-flow is required and not optional. **Do not buy grid pixels back out of a tap target.** **ADR renumbered `0024` → `0025`**: the responsive ADR claimed a number already held by `0024-brand-deposit-rate-fans-out-to-products.md` on an unmerged branch that this branch's own history contains — **an ADR number is free only if no BRANCH holds it (`git branch -a` + `git ls-tree`), not merely if `ls docs/adr/` misses it**; 0018 is likewise held by `feat/push-notifications-fcm`.
- **2026-09-07** — **bottom sheets need a height FLOOR in short viewports, not a plain fraction**: the receipt printer picker painted "BOTTOM OVERFLOWED BY 59 PIXELS" in landscape. All four call sites capped it at `context.getRHeight(0.5)` — a blind fraction, i.e. a 206dp ceiling on a 915x412 phone — while the sheet's own content needs ~265dp. **A sheet's chrome does not compress with the viewport**: the refresh `IconButton` and the paper-size `SegmentedButton` are both pinned at the 48dp tap-target minimum and `AppInput`/`AppDropdown` bottom out at 40dp, so intrinsic sheet height is roughly constant across orientations while `screenHeight * fraction` collapses 2.2x on rotation. **New seam**: `context.sheetMaxHeight(fraction)` in `responsive.dart` — the fraction as given, but floored at 0.90 when `isShortViewport`. Use it for EVERY `showModalBottomSheet` `maxHeight`, never bare `getRHeight`. Second, independent fault in `PrinterPicker`: only the device-list branch was `Flexible` — loading and empty were fixed-height `Padding`s with an **unscaled** `EdgeInsets.all(32)`, so the content could neither shrink nor scroll. Every branch is now `Flexible` + `SingleChildScrollView`, which is what makes the widget safe under ANY cap (regression-tested against the old 206dp ceiling). **10 more `getRHeight(0.x)` sheet sites carry the same latent bug** — see the responsive plan §10 gap 5. Gotcha for anyone widget-testing overflow: the `flutter_test` font renders every glyph as a square of the font size, so a 22-char title measures ~387dp instead of ~190dp and manufactures horizontal overflows that do not exist on device — the header title is now `Flexible` + ellipsis, which is also real protection at a large accessibility `textScaler`.

## August 2026
- **2026-08-20** — **the cart now reads the brand deposit rate live, and says so when it is unset**: fixes ADR 0024 defect (4) at the read site. A cart line snapshotted `emptyCrateValueKobo` / `trackEmpties` / `unit` / `manufacturerId` at add-time and nothing ever refreshed them — `CartService.refreshProduct` existed for exactly this and **had no production caller**, only a test. Meanwhile `createOrder` re-reads the product AND `manufacturers.deposit_amount_kobo` before booking crates, so the cart quoted one deposit while the sale recorded another (the mis-classified part deposit in `crate_return_modal.dart`). **New seam**: `CatalogDao.resolveCrateConfig` (one-shot) + `watchCrateConfig` (live) — one LEFT JOIN products→manufacturers returning `CartCrateConfig`; the deposit is the MANUFACTURER's rate, the product mirror is read **only** when a product has no manufacturer. `CartService.syncCrateConfig(map)` re-stamps every bucket but returns true **only** when the ACTIVE cart moved (a parked store's cart must not interrupt the cashier), and returns false / notifies nobody on a no-op so a screen re-syncing on cart change cannot loop. `CartCrateSync` (provider-held, `start()` idempotent) follows the cart's product set with the live query — **the cart screen is mounted once into MainLayout's tab stack, so mount-time reads never see an Inventory edit**; the Checkout button also awaits a one-shot `sync()` and, if anything moved, stays on the cart rather than pushing a CheckoutPage that snapshots its totals at construction. **Gotcha found while extracting**: the checkout quote was built from locals captured in `build`, so the page got whatever the last paint computed — now derived on demand via `_quote()`/`_CartQuote`, shared by the display and the hand-off. **New warning banner** on cart + checkout naming the products/brands whose crate value is 0; it does **not** block — a 0 rate collects no deposit and falls through to crate-tracking, which is the safe outcome, but it silently values the crates at ₦0 on the order and on the customer's balance. Bottle-detection + the unconfigured rule moved to pure `lib/core/crates/cart_crate_lines.dart` (widget-harness-free tests). **ADR 0024 amended**: its "changes van sales and the receipt builder" estimate was wrong — neither ever read the mirror, the cart was its only money-path reader.
- **2026-08-11** — **Post-login landing is per role, and it cannot be decided at login**: CEO / Manager / Stock keeper land on Home (tab 0), Cashier lands on POS (tab 1); a Driver is unaffected because #142 routes them to `DriverTerminalScreen` *instead of* MainLayout. **This reverses the 2026-06-19 "everyone lands on POS" decision for CEO and Manager**, at the owner's request. **The structural fact worth keeping: `AuthService.setCurrentUser` runs BEFORE the role row has resolved from local SQLite, so it cannot know the role** — it now calls `NavigationService.beginSessionLanding()`, which parks the session on **Home**, the one tab that is never hidden from any role's nav bar and therefore the only safe answer for an unresolved role. MainLayout applies the real landing via the new **one-shot** `applyRoleLanding()` once `rolePermissionsProvider` has a value. One-shot matters: MainLayout re-schedules it on every build, and a second application would yank a user back to their landing tab after they had walked off it; the move is also skipped when the session is no longer sitting on the untouched Home default, so a tab picked *inside* the resolve window survives. Net effect is **less** flashing than before, not more — previously all four roles landed on POS and the Stock keeper was visibly bounced out; now only a Cashier moves, and that move lands under `_HomeRouter`'s 350 ms `AnimatedSwitcher` fade. **Hard rule #7 is folded into the landing but kept as a separate continuous guard**: the landing computation passes Home whenever `Gates.makeSale` denies (a Cashier stripped of `sales.make` never opens on a POS tab hidden from their bar), while the pre-existing bounce-off-tabs-1-and-8 check stays un-latched so it also catches a permission revoked live mid-session. **Latent bug fixed on the way**: `handleBackPress` step 3 hardcoded "not POS → jump to POS", which bounced a Stock keeper onto the very tab MainLayout then had to bounce them back out of; it tracks `NavigationService.landingIndex` now. **The rule is deliberately NOT a `Gates` entry** — a landing tab is a preference, not a gated action (no denial feedback, no write boundary), so a registry entry would misuse ADR 0002; it lives as the pure `NavigationService.landingTabForRole(slug)` next to the tab-index constants that service already owns. New `test/shared/role_landing_tab_test.dart` (10) covers the map, the latch, and the logout re-arm.

## July 2026
- **2026-07-30** — **#186 one event, one basis: the variance card's money stops arguing with its own units**: #182 froze the count-shortage money at the write-time FIFO snapshot and, without meaning to, left the money and the units standing on different *selections*. Money summed **every** count-reconciliation `stock_adjustments` row in the period on the row's `created_at`, undeduped; units / retail / per-product lines / products-counted came from the count **SESSION**, deduped to the latest per `businessDate|storeId` on `businessDate`. Two divergences fall straight out: a **same-day recount** charges the day twice in money and once in units (so a recount that comes out matching still raises an integrity flag over money the latest count says is not missing), and a **backdated / after-midnight count** puts its units in one period and its money in another. **Decision: the money follows the units — the winning session, bucketed on `businessDate`** (ADR 0014 now carries a "which basis each figure stands on" amendment with both axes as a table: *valuation* current-cost vs write-time snapshot, and *selection* all-rows vs winning-session, which had never been written down at all). Three reasons in ascending weight: it is the only reading where a matching latest count reads **zero** variance; **count surplus cannot move to the rows** (a gain draws no FIFO batch, so no snapshot exists) and `variance = surplus − shortage`, so only a session-based shortage puts both halves of that subtraction on one basis; and `productsCounted`/`shortageCount` are facts **only a session holds** — no adjustment row knows how many products someone counted. **The cumulative reading is not lost and that is why the stock card was left alone**: "Count corrections" still sums every count of every day at current cost, is now the ONLY place that view lives, and says so on screen and in the export ("every count" vs the P&L's "latest count per day"). **#182 is untouched** — rows are still valued at their snapshot; only the selection moved — and the flow-equation terms are untouched too, so `stockDerivedClosingKobo` still ties to the rewound perpetual figure by construction. **No schema change, and the reason is reusable**: `stock_adjustments` has no `count_id`, but `_saveCount` writes each changed line's adjustment IMMEDIATELY BEFORE `recordCount`, so a row belongs to the earliest session of its own store whose `created_at` is not before the row's — a `created_at` window recovers the link a column would have stored. Its imperfections are documented rather than hidden (a save whose `recordCount` never ran rides on the NEXT count — dropping those rows would hide stock that really moved; two saves inside one second are indistinguishable because both tables store whole seconds, which is why the tests stamp their own timestamps; a null-store session owns nothing since `stock_adjustments.store_id` is NOT NULL). A winning session with **no** attributable rows values its own count lines at current cost and counts every one into the existing `legacyValuedShortageRows` footnote — money and units must never disagree about whether a shortage happened *at all*, and a labelled fallback beats a silent ₦0 beside 5 missing units. Day-close is unaffected by construction: the frozen figure set's only count figure is `shortageUnits` (session-sourced all along), and the list sweep's per-day row slices can push a backdated session onto the labelled fallback without moving any frozen figure. Tests 13 → **27** across the two shortage suites; mutation-verified by restoring the old basis (4 of the 6 new roll-up assertions go red: 30000/0, 50000/20000, 0/40000, 0/60000). **1800 pass / 128 skip / 0 fail**; `flutter analyze` clean. Still latent, unfixed, on the flag-flip checklist: if `feature.domain_rpcs_v2.inventory_delta` is switched on and the server-minted row carries no `value_kobo`, shortage silently reverts to current cost — the same dependency #170's damages carry.
- **2026-07-30** — **#219 `orders.total_amount_kobo` means THE PAYABLE on both sync paths, and the migration had to jump to 0177**: v1 (`OrderCommands.checkout`) writes the payable — `gross − discount + deposit` — into **both** `total_amount_kobo` and `net_amount_kobo`; `pos_record_sale_v2` was writing `v_total_amount` (the GROSS) into the total while already writing the payable into net, so **only the total column diverged**. The fix is therefore `v_total_amount → v_net_amount` in the `orders` INSERT, NOT "set both to a new expression". `receiptTotalsFromOrder` states the column's meaning outright and reconstructs the pre-discount goods as `total − deposit + discount`, so a v2 row fed to it reprinted a discounted sale's Subtotal high by the discount — #200's defect through the other sync path. Forward-only: **no restatement, no backfill**. Cloud **0177** (`CREATE OR REPLACE`, signature byte-identical to 0173's — a changed signature would mint an overload and break the client with PGRST203). **Numbering gotcha worth keeping**: 0174 was free (main renumbered #216/#217 to 0175/0176 around #209's 0173) but **unusable** — 0175/0176 are already APPLIED on the cloud, so a migration numbered below applied history is out-of-order and `supabase db push` rejects it. Neither 0175 nor 0176 redefines `pos_record_sale_v2`, so sitting above them cannot revert this. Still latent: `feature.domain_rpcs_v2.record_sale` is held OFF on #121, and the Tier-2 expectations pinning this have **never executed anywhere** — flag-flip checklist. Unfiled sibling: the web POS's `checkout_order` (0139) has the same gross-in-`total_amount_kobo` divergence and that path is **LIVE**.
- **2026-07-30** — **#207 slice 1 values lost crate shells, and the value is DISCLOSURE ONLY**: `computeVanTripPosition` gains `shellsLostUnits` / `shellsLostValueKobo` / `shellLosses`, fed by a new `VanPositionShellLine {manufacturerId, shellsOut, shellsBack, depositRateKobo}` input. **The grain is the manufacturer, not the lot and not the product**, because the floor has to be applied where shells are fungible: 40 Star out on the morning lot + 20 on the afternoon restock, 55 back, is **5 lost, not "0 and 5"** — a rate on `VanPositionLot` would floor per lot and a product-keyed map per SKU, both wrong for one physical crate. Duplicate `manufacturerId` lines are folded **before** the floor, so a later slice may pass one line per lot/return event (the grain `van_trip_lots.shells_out` / `van_return_events.shells_back` actually store) and get the same answer. Loss floors at 0 — more shells back than out is not negative loss — while the raw `shellsDifference` memo stays unfloored and can go negative. **The money constraint**: the value is in NO existing figure — not `balance`, `outstanding`, `shortageOwedKobo`, `cogsKobo` or `profitKobo` — matching the `shortageLossKobo` "Disclosure only." precedent. Whether a driver OWES for lost shells amends ADR 0019 and needs a `driver_ledger_entries` type; until then this is a number you read, not one you settle. Guarded by a test that builds the same trip with and without shell lines and asserts **16 money figures byte-identical**; folding the value into `shortageOwedKobo` fails it. Production value stays 0 until `VanSalesDao.positionForTrip` joins the rate through `products.manufacturer_id → manufacturers.deposit_amount_kobo` and passes `shells:` — the next slice.
- **2026-07-30** — **#208 van v1 polish: one receipt widget, a van fallback for dormant drivers, and close now blocks behind an override**: (1) The road receipt's print/share plumbing (`ScreenshotController`, `ThermalReceiptService`, `PrinterPicker`, `share_plus`) lived inline in `driver_terminal_screen.dart`; it is now `VanReceiptView`, used by both the terminal and `VanSaleReceiptSheet`, so a manager auditing from the driver profile can reprint. **Side effect accepted deliberately**: the terminal's ON-SCREEN receipt now carries the business-name header it always lacked (the thermal print and the profile sheet both had it) — the shared widget makes the three agree. `cashReceived` is unchanged on the terminal (`sale.totalKobo`) and correct on the sheet (`order.amountPaidKobo`); the reopened copy stamps REPRINTED/RESHARED per the `orders_screen` convention. (2) A driver's vans still resolve from their **trips** (a removed driver has no store assignments left, and their history is the point); `user_stores` is consulted **only** when that resolves to nothing — the dormant-account case where every trip predates the pull window — filtered through vans-only so a warehouse assignment can never be read as a van. (3) Close now **disables** Confirm while this device's outbox is dirty, with a quieter "Close anyway" routed through the ordinary close dialog retitled "Close on an incomplete picture?" (`Wait for sync` / `Close anyway`). **Deliberately not a hard block**: sync can be down for days (errno 7) and an un-closable trip strands the van, the driver's balance and the next dispatch, while a late sale posts its own correction (§9.4 #15). Every string still says **"this device"** — a device reads only its own `sync_queue` (#145). **The override is NOT recorded on the artifact**: `van_trips` has no column for it (`restatedAt`/`restatedReason` belong to the late-sale restatement and `closedWithBalance` means something else), and none was invented; spec §7.4 records the gap. (4) `vanDriversProvider`'s name ordering is a **decision, not a gap** — `Roles` carries `UNIQUE (business_id, slug)` (cloud `ON CONFLICT (business_id, slug)`, 0161), so the driver-role set is a singleton by schema and a tier comparator is *constant*: tier-then-name returns the name order already there while falsely implying a mixed list. Pinned by a test that fails first if a second driver-ish role is ever seeded.
- **2026-07-30** — **#217 a customer forfeit nets to zero on an opted-in brand, and the no-restatement guarantee is STRUCTURAL, not arithmetic**: PRD #203's last slice (ADR 0023 finding #4 / rule 5). A kept crate deposit stays income (#176) on every brand; on a brand whose Crate Money Arrangement moves money, `OrdersDao.settleCrateDepositReturn` now also calls `CratePoolDao.recordCustomerForfeitShortfall`, which books the crates KEPT as a `crate_shortfall_writeoffs` row at the manufacturer rate snapshotted then — so gain and loss are the same size and profit does not move. The netting is decided **once, inside the settling transaction**, and persisted; **no report re-derives it from today's setting**, which is the whole reason switching a brand on next Tuesday cannot restate last March (ADR 0021). A reader that re-decided from the current arrangement would rewrite every closed day on a switch flip — do not add one. Count is `kept`, never the capped `forfeitRecorded`: the crate costs the depot rate whatever the customer put down. Id is `UuidV7.deterministic('crate_forfeit_netting:$orderId:$manufacturerId')` (#188's trick) so two offline tills collapse instead of booking the loss twice, and the seam inserts `insertOrIgnore`. Drift **v81** + cloud **0176** add `crate_shortfall_writeoffs.source` (`manual` | `customer_forfeit`, NOT NULL default `manual`, cloud CHECK only, no backfill and none may ever be added); it is display/disclosure only — both origins hit the same P&L line — but it keeps "who accepted this loss and when" naming a human (`_lastManualShortfallWriteOff` filters to `manual`) and feeds `ReconData.crateForfeitNettedKobo`, which is **inside** `crateShortfallWrittenOffKobo` and must never be subtracted twice. Release gate held: `movedByThisSlice` gained exactly one key, and `forfeitIncomeKobo` was added to `figures()` to pin that the income itself does NOT move.
- **2026-07-30** — **van sales was hardcoded to drinks; the item noun is a Lexicon slot, empty crates are a GATE**: two leaks that look like one and are not. (1) Every user-visible "drink"/"drinks" in van sales is now `ref.watch(industryLexiconProvider)` — `lex.item` / `itemLower` / `itemPlural` / `itemPluralLower` (ADR 0015) — because **van sales sells the same catalogue the shop does**, so it must caption it with the word the rest of the app already uses. Note this changes BARS too: Beverage/Bar resolve to `item: 'Product'`, so van sales stops being the one surface that says "Drinks" while inventory and the POS say "Products". (2) **Empty crates are not a Lexicon slot and must never become one** — there is no pharmacy word for a crate, which is exactly why `lexicon.dart`'s own doc says beverage-only nouns live behind `isCrateBusiness` instead. Van sales was simply the one feature that never applied that gate, so a pharmacy running vans was asked "Empty crates going out (optional)" on every load line and got a **Crates tab** on every driver. All crate surfaces now sit behind `businessTracksCrates` (the combined type + opt-in gate): the load/return shell fields, the reconcile shell-memo card **and its line in the close dialog**, the driver-profile Crates tab (`DefaultTabController` length 4 → 3 **in lockstep with the TabBar and TabBarView** — the three must move together or Flutter throws), and the driver terminal's swap-only banner. **DAO messages cannot use the Lexicon** (it is a UI seam and a DAO holds no `ref`): `VanOverReturnException` was rewritten to name no item noun at all rather than pick one that reads wrong outside a drinks business. **No schema/migration/sync change** — `shells_out`/`shells_back` still exist and still write, they just stay 0 for a trade with no empties. Guard: `test/van_sales/van_sales_trade_neutral_test.dart`, which asserts the crate gate **both ways** (gone for a pharmacy, still there for a bar — a one-way assertion would be satisfied by deleting the feature) plus a source ban on drinks nouns in van-sales string literals; proven by reintroducing both bugs and watching 6 of 8 go red. Gotcha worth keeping: the reconcile screen is a `ListView`, so widget tests must set a tall `tester.view.physicalSize` or the shortage/shell/uncosted cards are never built and every assertion about them passes vacuously.
- **2026-07-30** — **discoverability hints stop reappearing after a ✕: the retire contract is now uniform across all five banners (PR #148, authored 2026-07-19, merged today)**: `UiHintService`'s contract is `markShown` = count one passive VIEW (auto-retire after `_retireAfter = 2` displays) and `markDismissed` = deliberate ✕ close (retire IMMEDIATELY). The five inline banners each used it differently, and the two mistakes were mirror images: **POS (both banners) and Receive called `markShown` on the ✕**, so a deliberate close only moved the count 0 → 1 and the banner **surfaced one more time** — the parked "hint shows double" report; and **POS / Receive / Inventory never counted a passive view at all**, so an ignored banner reappeared on *every* visit and only a ✕ ever retired it. Only Cart dismissed correctly, and only Cart counted views — no single screen had both halves right. Fixed at the call sites; **`UiHintService` itself is unchanged**, which is the point: the service was never wrong, five callers were. **The POS half needed a merge decision, worth stating because the log already contains the other side.** The 2026-07-21 entry below records POS's two stacked banners being combined into ONE widget — but they stayed on **two independently retiring keys** (`hintPosTapAdd` + `hintPosLongpress`), which is exactly what made the ✕ a *half*-dismiss: one close, two `markShown` calls, both counts 0 → 1, banner back once more. So the keys are now merged into a single **`hintPosGestures`**, and that entry's key list is superseded. **What that cost, deliberately:** `_posGridHintMessage`'s adaptive copy (both-gestures / hold-only / tap-only sentences, "whichever tips are still live for this user") is gone. Those branches only had distinct reachable states *because* the two keys retired independently — the defect itself — so merging the keys collapses them to the both-gestures sentence by construction, not by omission. The 07-21 wording ("choose **a** quantity", not "the") is kept: POS long-press opens the quantity/discount sheet to add several at once, it does **not** edit the product (only Inventory's hold-to-edit banner, #110, really edits). Net behaviour, now identical on all five: shows on up to two ignored visits then self-retires, and **one ✕ retires it for good**. Merge note for anyone re-reading the diff: the branch was 163 commits behind main and conflicted in two files — `ui_hint_service.dart`, `inventory_screen.dart`, `cart_screen.dart` and `receive_stock_screen.dart` all auto-merged, and `BUILD_LOG.md` conflicted only because the branch predates the log's condensation into `build_log_history.md`.
- **2026-07-30** — **#209 the v2 sale envelope stops dropping the customer's crate credit (cloud `0173`)**: `pos_record_sale_v2` is server-authoritative on totals — it recomputes the gross from `p_items` and derives the payable itself — but it derived it from **one** client-sent reduction, `p_discount_kobo`. The cart's goods total is `gross − discount − crateCredit`, so the credit had nowhere to ride and vanished on the wire, and `v_net_amount` came out higher than the header the device had already written. **One dropped term moved two numbers**: the RPC's response overwrites the local order header (`_applyDomainResponse` → `_restoreTableData('orders', …)`, so a credit sale recorded a payable the customer was never charged — this half predates #175), and #201's tender split divides `v_net_amount − depositHeld`, so an over-stated goods payable books up to `credit` MORE as goods `sale` and correspondingly less as `wallet_topup` than v1 books for the same sale. 0170's header had named this and filed it, calling the split "the only self-consistent choice available **without a signature change**"; this is the signature change. **One new parameter and one line of math** — `p_crate_credit_kobo int DEFAULT 0`, `net = gross − discount − credit + deposit` — with the whole function body copied VERBATIM from 0170 and the two deltas marked `-- #209 delta`. **It is a SEPARATE term, deliberately not folded into `p_discount_kobo`**, because that parameter is STORED as `orders.discount_kobo` and read back as a discount: the receipt rebuilds Subtotal → Discount → Total from it (`receiptTotalsFromOrder`, #200/US 33) and margin review reads it as revenue given away, whereas a crate credit is the customer's own deposit money coming back. Folding them would have made `net_amount_kobo` right and the discount column wrong — a visible defect traded for an invisible one. It also gets **no `orders` column**: `net_amount_kobo` is the credit's entire effect, and a column would cost a Drift bump + sync-registry entry + pull-restore case for a figure the client re-derives; when a real credit ships and needs itemising, that is the change to make, with its own printed receipt line as `ReceiptTotals.totalKobo` already says. **The client DERIVES the value rather than taking an argument** — `OrdersDao.createOrder` computes `Σ(qty × unit_price_kobo) − discount_kobo − (total_amount_kobo − crate_deposit_paid_kobo)`, i.e. "the part of the payable the header does not otherwise explain" — so any future cart reduction rides the envelope automatically and this seam cannot silently drop one again, while the client still never asserts a total (the RPC stays the authority on gross and on every line's price). Signed on purpose: a term that RAISED the payable rides as a negative instead of being clamped into a fresh divergence. **Deploy order is free in both directions TODAY and that expires**: the parameter has a DEFAULT, and the client OMITS the key whenever the credit is 0 — which is always, since #202 deleted the cart block that computed one — so today's envelope is byte-identical to the pre-#209 one and a client ahead of 0173 never sends an unknown parameter (PGRST202 ⇒ jammed sale outbox). The moment a real credit is wired up, **0173 must be deployed first**. **Overload trap observed** (memory `project_rpc_param_add_overload_trap`): adding a parameter with `CREATE OR REPLACE` mints an OVERLOAD and PostgREST then fails every call with PGRST203, so the 18-argument signature is DROPPED first and re-GRANTed after (DROP takes the grants with it) — the same dance 0164 and 0045 did. **NOT deployed**: `migration list` shows 0171/0172 (PRD #203 slices) still un-applied on prod, so a `db push` would carry them too; 0173 is inert until `feature.domain_rpcs_v2.record_sale` flips (still held off on #121), so it waits. **Adjacent divergence found and deliberately NOT fixed here, FILED as #219**: the RPC writes `orders.total_amount_kobo := v_total_amount`, the **gross**, while the v1 path pushes the **payable** and `receiptTotalsFromOrder` reads that column as "already NET of the discount and INCLUSIVE of the deposit" — so on ANY discounted v2 sale the mirrored header over-states the goods by the discount and a reprint's Subtotal is inflated by it. Different root cause (a column-meaning disagreement, not a dropped term), not latent behind the crate credit, and the fix moves every v2 sale's header, so it needs its own decision; the Tier-2 test currently pins today's behaviour (`reason: 'server computes total = sum(qty * unit_price)'`). #209 lands `net_amount_kobo` correctly on both paths — `total_amount_kobo` is the one still diverging, and it stays on the flag-flip checklist until #219 closes. Tests: 4 dispatch tests build the header a credit sale WOULD write and pin the contract (discount-only forwards nothing; a payable below `gross − discount` forwards the gap; the deposit is added back BEFORE the credit is derived so held money is never mistaken for a credit; the v1 path is untouched) plus the byte-identical guarantee, and 2 Tier-2 RPC tests pin the server half. Mutation-verified (removing the payload key turns exactly the two credit assertions red). **1753 pass / 128 skip / 0 fail** on main after the merge (1749 / 126 before it: +4 dispatch, +2 skipped Tier-2); `flutter analyze` clean. **NOT deployed** as of this entry — corrected below.
- **2026-07-30** — **cloud 0171 + 0172 + 0173 DEPLOYED to prod**: pushed together after a `--dry-run` listing exactly those three, in dependency order. **0171** adds `manufacturers.crate_money_arrangement` (#211, PRD #203 slice 2); **0172** adds the Placed Deposit tables (#212); **0173** adds `pos_record_sale_v2`'s `p_crate_credit_kobo` (#209). 0171/0172 had been merged to main since 2026-07-29 but never pushed, so #209's deploy carried them — worth knowing that a `db push` is repo-wide, never per-issue, so the pending set must be read before pushing and not just the migration you wrote. **Verified against prod afterwards, not assumed**: `pos_record_sale_v2` returns **exactly one row** in `pg_proc` with **19 args** including `p_crate_credit_kobo integer` (so the 18-arg `DROP` bit and left no PGRST203 overload — the signature was checked against the deployed one FIRST, since a mismatched DROP is a silent no-op), `prosrc` carries the `COALESCE(p_crate_credit_kobo, 0)` term, and EXECUTE is back for both `authenticated` and `service_role` (DROP takes grants with it). **0171's release gate confirmed on live data**: all **24** manufacturers landed on `'none'` with nothing backfilled, and `supplier_crate_deposits` has **0** rows — so PRD #203's later slices have nothing to act on and no tenant's figures moved. Security advisors: **0 ERROR**, 111 WARN, byte-identical in count and class to the 2026-07-29 baseline (12 `function_search_path_mutable`, 97 `*_security_definer_function_executable`, 1 public-bucket listing, 1 leaked-password) — nothing introduced. **Still unexercised, same caveat as 0170's deploy**: this was 0173's first execution anywhere (no local Postgres in the session), and `feature.domain_rpcs_v2.record_sale` remains OFF — schema-level correctness is confirmed, runtime behaviour is not. The 2 Tier-2 cases that would prove it need `TEST_USER_REFRESH_TOKEN`.
- **2026-07-29** — **A bottom sheet must OWN its `TextEditingController`s — `showModalBottomSheet`'s future completes on `pop`, not after the exit animation (Settings → Stores "Add a van" crash)**: `_addVan` created the controllers, handed them to the sheet, and disposed them on the line after `await showModalBottomSheet(...)`. That looks safe and is not: `Navigator.pop` completes the pushed route's future **synchronously**, while the route still has ~250 ms of reverse transition to run and the sheet's `TextFormField`s are still mounted and rebuilding every frame. They then touched disposed controllers → **`A TextEditingController was used after being disposed.`** on every single add-a-van attempt. **The durable rule: whoever creates a controller disposes it, in the same `State`** — a sheet returns *values* (here a small `_NewLocation`), never borrows a caller's controllers. Note the assert fires from `ChangeNotifier.addListener` via `_AnimatedState.didUpdateWidget` (the transition rebuilding), NOT from a `TextField.build`, which is why the stack looks unrelated to the sheet. **`removeListener` is explicitly legal post-dispose and `ValueNotifier.value` does not assert, so a "dispose after await" bug does not always throw** — it throws when something re-registers a listener, i.e. exactly when an animation is still running; a sheet with no exit animation would have hidden it. Regression-proved rather than assumed: reinstating the disposal turns 5 of the 7 new `test/settings/stores_settings_screen_test.dart` cases red with the user's verbatim message, and `pumpAndSettle` after the tap is the part that drives the fatal window (a test that stops at `pump()` passes against the bug). **Also swept**: this sheet held the app's ONLY `deviceBottomInset` call site — the helper whose own doc says it is "almost always the WRONG choice" and that "the app has no such call sites today" — reverted to `deviceBottomPadding`.
- **2026-07-29** — **"Every debug run signs me out" was the emulator's disk, not auth code — `INSTALL_FAILED_INSUFFICIENT_STORAGE` makes `flutter run` fall back to uninstall+reinstall, which wipes flutter_secure_storage**: worth writing down because the symptom points squarely at the auth stack and the cause is nowhere near it. The debug APK is **179 MB** (`assets/flutter_assets/kernel_blob.bin` alone is 117 MB — inherent to a debug build, not asset bloat), the AVD's `/data` was **91% full with 579 MB free on a 6 GB partition**, and an in-place update needs old+new to coexist *plus* clears Android's low-storage reserve (~5–10% of the partition). So `adb install -r` fails, the Flutter tool logs `Uninstalling old version...` and reinstalls clean, and `/data/user/0/<pkg>` goes with it: `secure_device_user_id` (flutter_secure_storage), the Drift DB, and the persisted Supabase session. `_HomeRouter` then reads `hasDeviceUser == false` and lands on `WelcomeScreen` — a total sign-out, every run, with no code change involved. **Two cheap tells that identify this without reading a build log**: `adb shell dumpsys package <pkg> | grep -E 'firstInstallTime|lastUpdateTime'` — **equal timestamps mean a fresh install, not an update** — and `adb shell df -h /data`. Run `flutter run -v` and grep for `Uninstalling old version` to confirm. **Diagnostic dead end worth skipping next time:** the synced `error_logs` breadcrumbs (`auth.session_lost` / `auth.session_expired_gate`) are useless for this class of bug — the wipe destroys the unpushed rows and the session needed to push them, so the table just looks stale. `pm trim-caches 4096M` recovered only 45 MB; the fix is capacity (`disk.dataPartition.size` in `~/.android/avd/<name>.avd/config.ini`, or a fresh AVD), and note **`adb root` is refused on `google_apis_playstore` images**, so you cannot `du` your way around inside `/data`.
- **2026-07-29** — **Multi-store was fully supported downstream but no surface could CREATE a store — `StoresDao.createStore` had exactly one caller, and it always passed `kind: 'van'`**: store lock, transfers, per-store ledgers/reports and `withoutVans`/`onlyVans` all handled N stores, yet a business was permanently stuck with the single row onboarding minted, and Settings → Stores said "Adding more stores is coming in a future update" (retired). The van registration desk had been built on top of `createStore` without ever exposing the plain-store case beside it. Settings → Stores now offers **Add a store** next to **Add a van**, both through one `_addLocation({required bool isVan})` — so the fire-time gate (`stores.manage` for a store, `van.manage` for a van; hard rule #6), the `kind`, the activity-log action (new: `settings.store.create`) and the copy cannot drift into two half-maintained branches. `Gates.manageStores`' doc already said "the Stores FAB", so the gate was waiting for this. Still true and unchanged: a van stays out of every store picker and per-store report purely via `kind`, which the new tests pin from both directions (a store never appears in `onlyVans`, a van never in `withoutVans`).
- **2026-07-29** — **Cloud 0168 + 0169 + 0170 DEPLOYED to prod (the #155 close-out follow-up batch)**: pushed in dependency order after a clean `migration list` (local == remote through 0167; 0159 correctly absent, parked on the FCM branch) and a `--dry-run` that showed exactly these three. **0168** adds `stock_adjustment_requests.unit_cost_kobo` **bigint** (#197 US 22 — bigint, not int4, or ₦21.4M+ upserts jam the outbox); **0169** narrows the `payment_transactions` type CHECK by dropping `'purchase'` (#202); **0170** brings the three flag-gated RPCs to #155 parity (#201). **Verified against prod afterwards, not assumed** — this matters because 0170's own "verification block" is a COMMENT, not executable SQL, so a clean apply proves nothing on its own: `pg_proc` returns **exactly one row per function** (`_apply_stock_adjustment` 7, `approve_stock_adjustment` 4, `pos_cancel_order` 4, `pos_record_sale_v2` 18, `request_stock_adjustment` 8) ⇒ **no overloads survived**, which was the real risk — both `DROP FUNCTION IF EXISTS` signatures were checked against 0141's deployed `CREATE` signatures beforehand, since a mismatch makes the DROP a silent no-op and leaves 6-arg calls binding to the stale cost-less `_apply_stock_adjustment` (worse than PGRST203: silent, on a REVOKEd function). `unit_cost_kobo` confirmed `bigint`; the type CHECK now reads `sale, expense, refund, wallet_topup, crate_deposit, van_remittance` with **`purchase` gone**; and `SELECT count(*) … WHERE type='purchase'` = **0**, so 0169 genuinely NARROWED rather than taking its warn-and-skip path — worth pinning because that skip is one-shot on both halves, and a skipped narrowing needs a NEW migration, never a replay. Security advisors: **0 ERROR**, 111 WARN, all pre-existing classes (`function_search_path_mutable` ×12, `*_security_definer_function_executable` ×97, one public-bucket listing, one leaked-password setting) — none introduced here. **Still unexercised:** this deploy was 0170's FIRST execution anywhere (no local Postgres/Docker in the session, so the SQL was never run before prod), and the `feature.domain_rpcs_v2.*` flags remain **OFF** — schema-level correctness is confirmed, runtime behaviour is not. The Tier-2 arm that would prove it needs a fresh `TEST_USER_REFRESH_TOKEN`. **Gotcha for the next deploy:** `supabase db dump` shells out to Docker, so it is unavailable when Docker is down — use the Supabase MCP `execute_sql` for post-deploy verification instead.
- **2026-07-29** — **#206 the golden fixtures finally pin CANCEL and the catalogue price (ADR 0009, PRD #155's hard rule)**: two money rules #155 changed were never pinned in the shared fixtures, and one of the gaps had been papered over in this very log. **(1) There was no cancel scenario at all** — #172's compensating cash-out, the most consequential money change in the PRD, had **zero** cross-implementation contract. New `test/golden/fixtures/cancel_scenarios.json` (3 scenarios) extends a scenario with an optional `cancel` block: the runner checks out, asserts it with the usual `expectGolden`, then cancels and asserts the compensated state with the new **`expectGoldenCancel`** — so every reversal is measured against a sale whose own rows are already pinned. **The load-bearing assertion is the FULL multiset of payment rows on the order after the cancel, originals included** (`type|method|signed amount|voided`), because that one comparison carries both halves of the rule: the originals must still be there UNVOIDED and unedited, and exactly one compensating row must have been APPENDED per reversible original — 0170's own phrasing, *"row count after a cancel = originals + one per reversible original"*. It encodes **#190/#201's in-family reversal, not the old void-in-place rule**: `sale` → a positive `refund`, `crate_deposit` → a **negative `crate_deposit`**, `wallet_topup` → a **negative `wallet_topup`**. **One half of the rule is deliberately NOT in the fixture, because a fixture cannot express it:** "the reversal lands on the CANCEL day" only means anything because the ORIGINAL keeps its own `created_at`, and a golden run happens in a single instant, so the two dates can never be compared to each other — each runner instead snapshots every original row's full state (type/method/amount/`created_at`/`voided_at`) BEFORE the cancel and reports anything changed or vanished as `CancelOutcome.mutatedOriginals`, which the comparator requires to be EMPTY (mutation-verified with a `created_at` rewrite, which the row multiset cannot see). The day-boundary case itself stays pinned, with a backdated sale, in `cancel_cashout_test.dart`. Also asserted: every wallet leg after the cancel (originals + appended opposites — goods debit → `refund` credit, payment credit → `void` debit, held deposit → `crate_deposit_refunded`, #162) with the balance back at its pre-sale value; inventory restored; and the **restored FIFO layers** (#170 #7c) — units come back COSTED at the snapshotted COGS while the SEEDED batch stays drawn down, since topping it back up too would double-restore (#187/0167). **All three are `dart_arm_only`, and that is a statement about the RUNNER, not the rule**: `pos_cancel_order` implements exactly these expectations as of #201/`0170`, and **0170 was deployed later the same day** (see the deploy entry above) — so the cloud half of the condition is now met, and the sole remaining blocker is that the RPC runner still has no cancel step. Corrected in `cancel_scenarios.json` too, so nobody reads the stale "not deployed" line and concludes un-flagging is now safe. **Gotcha this design closes:** registering fixtures on an arm that has no step for them is how coverage gets overstated in the first place, so `checkout_order_golden_test` **`fail()`s loudly** on a cancel scenario — un-flagging without first building a `pos_cancel_order` step would otherwise assert only the checkout half and pass. The crate-track leg is deliberately NOT modelled: 0170's cancel writes no crate rows at all (ADR 0020 — the seam is the sole crate-table writer), and a fixture no second implementation can ever satisfy is not a contract. **(2) `catalogue_price_kobo` was asserted nowhere** — every scenario sold at full price, so the column was NULL on both arms and nothing was compared. A checkout line may now carry **`charged_unit_price_kobo`** (the price actually charged when the tier price was overridden) while the product's `unit_price_kobo` stays the CATALOGUE price; the new `custom-price concession (#176/#183)` scenario charges ₦800 against ₦1,000 **alongside a full-price line in the same cart**, so a real concession must be recorded AND a full-price line must record none. The column joins the per-line signature the comparator diffs, so every pre-existing scenario now asserts NULL too instead of ignoring it, and the rule is shared as **`catalogueSnapshotKobo`** beside `clampDiscountKobo` (the Dart twin of `OrderCommands._buildOrderItems` / SQL `_catalogue_price_snapshot`, 0160). **Gotcha worth stating: the RPC arm forwards the key on EVERY line, not just the concession one** — sending it only where a concession exists would leave the full-price lines NULL for the trivial reason that nothing was sent, pinning half the rule instead of putting `_catalogue_price_snapshot` under test in both directions. That scenario is deliberately **not** `dart_arm_only` (0160 IS deployed) — but the Tier-2 arm was **not run** this session, so it is pinned in the shared fixture and proven on one arm. Both new assertions were **mutation-verified** (encoding the pre-#155 positive-`refund` deposit release, and dropping the catalogue expectation, each turn the suite red). `flutter analyze` clean; golden suite 40 pass.

- **2026-07-29** — **#192 day-close delta covers all 16 frozen figures; snapshot can no longer freeze a crate business mid-load**: `dailyClosingFiguresFrom` freezes 16 figures but `ReconClosingComparison` compared **4**, so a reviewed day whose expenses/refunds/damages moved read as unchanged everywhere. Worst first — **`_frozenFiguresReady` (the data-corrupting gap)**: it named 8 streams while the frozen `netProfitKobo` also nets out THREE crate terms (`allManufacturersProvider` deposit rates, `allCrateDamagesProvider` stored-empty losses, `crateForfeitRowsProvider` kept deposits — the last one ADDED to profit), so a Bar's cold open could win the first-writer-wins race with a net profit missing them, **permanently**. The gate now mirrors the gather's watch set exactly: manufacturers + crate damages unconditionally (the gather already watches them, so nothing new is opened), forfeit rows **short-circuited behind `showCrates`** so a non-crate business is neither made to open nor to wait on a crate stream. Same slice closes ADR 0022's flagged cousin — the capture passes `isCeo: true`, not the viewer's role, so the frozen record is opener-independent in role terms as well as scope (no figure moves today; the flag gates only supplier-ledger flows, none of them frozen). **Comparison → 16 deltas**, one per column in `DailyClosingFigures` order, plus `all`/`anyChanged` and `dayLocal`/`anyDayLocalChanged` (the latter drops `stockExpectedClosing`, the only frozen figure rewound from TODAY's on-hand and so not derivable from one day's rows). **Badges are now a labelled LIST per card** in a `Wrap` under the title (expenses and damages move independently of net profit, so one unlabelled badge could not say which), and follow the §25.3 cost wall per role: a Manager gets Total sales / Refunds / Items sold on Sales, **Expenses on Debts & expenses** (the card that rendered the frozen `expensesKobo` with no delta at all), Short in units on stock, and an **amount-free "Stock value changed"** marker for the cost-basis figures — which also retires a pre-existing leak where the Manager's stock card showed a raw cost-basis money delta. **The banner is worded from what was actually flagged**: the per-card badge lists are built in `build` before the cards, so `anyChanged` can no longer promise "the flagged cards show what moved" with zero flagged cards; the third state reads "…but not on any card you can see here — ask the owner to open this day", which is exactly what a Manager gets for US 35's backdated supplier payment (cash card is CEO-only). **Outside the day**: new `changedReviewedDaysProvider` + `DailyClosingsDao.watchAllForBusiness` / `allDailyClosingsProvider` drive a "Changed since review" chip on the reconciliation LIST's Day buckets. It is **one pass over the data, not one per day** — rows are filed under the reviewed days they could report on (`_byCandidateDay`, deliberately over-inclusive since `reconDataFrom` applies the real date basis itself), then each day runs through the REAL `reconDataFrom` over its own slice: `O(rows) + O(days × products)` instead of `O(days × rows)`, which is the difference between a sweep and a freeze on a shop with a year of history. `businessWideStoreFilter(VanStores)` factored out of `reconStoreFilter` so the sweep shares the one definition of business-wide. **Deliberately NOT on the Home attention dot** — `reports_attention.dart` re-derives on every sale; this sweep is priced for a report screen you are already reading, not for Home. Tests: `recon_day_close_delta_coverage_test.dart` (pure) perturbs each of the 16 frozen columns and demands **exactly one** delta fires (0 = the hole, >1 = two deltas on one column), plus the US 35 case that had no test at all; `daily_reconciliation_detail_screen_test.dart` is the **first widget test this screen has ever had** (trigger: finished-day / permission / data-ready / one-shot; crate vs non-crate readiness; badge + banner per role). Mutation-verified both ways. Both surfaces render the SAME `ChangedSinceReviewBadge` (`lib/features/dashboard/widgets/`) rather than two hand-rolled pills. ADR 0022's "known cousin" bullet is struck through and corrected: the feared Manager-vs-CEO capture divergence was never real (`cashSupplierPaidKobo` was always summed outside the `isCeo` gate, and no gated term is frozen) — the flip makes opener-independence structural instead of coincidental. **Gotcha:** the badge chip's `Row` overflows inside the `Wrap` on a 375px phone unless its `Text` is `Flexible` — caught only because the widget test pins a phone surface (2026-07-27 entry). **Second gotcha, from review:** a badge can outlive the line it names — `Refunds` renders only when non-zero, so a refund VOIDED after the review badged a line the card had stopped drawing; the line's render condition now includes its own delta.

- **2026-07-29** — **#201 the three flag-gated server write paths reach #155 parity (cloud `0170_flag_gated_rpc_money_parity.sql`)**: three server-side money writers still implemented the **pre-#155** rules. All three were unreachable (two behind flags held off by #121, one on the web arm), which is exactly what made them dangerous — a flag flip reverts rules #155 had just fixed, with no code change to review. Fixed ahead of the flag, the precedent being `0160`/`0164` ("a latent landmine otherwise"). **(1) `pos_record_sale_v2` bundled one payment row.** It inserted ONE `sale` row for the whole `p_amount_paid_kobo` with **no `store_id`**, and on the v2 path the client writes no local payment row at all, so that was the only row — a flip would have re-bundled crate deposits and overpayments into "Cash sales" (undoing #175) and dropped the tender's store (undoing #169 US 36 / #194). Now the SQL twin of `_insertCheckoutPaymentRow`: up to THREE store-stamped rows — `sale` (goods paid = paid − deposit, capped at the goods payable), `crate_deposit` (the deposit HELD), `wallet_topup` (the overpayment) — always summing to what was tendered, a zero leg writing no row (so a goods-only sale is byte-identical to before), and a ROAD sale still writing none (#142). **Gotcha the RPC inherits**: the v2 envelope forwards only the per-line discount, never the customer's crate CREDIT, so on a credit sale `v_net_amount` exceeds the client's payable and the split books more as goods — a pre-existing v1/v2 envelope gap (it already moves the order header's own `net_amount_kobo`), recorded in the migration header as a follow-up. Side effect worth knowing: this closes one of the four cases #190 recorded where Confirm's cash branch finds no `crate_deposit` collection row to copy its tender from — a v2 sale now leaves one behind. **(2) `pos_cancel_order` voided in place AND double-reversed.** `0045` set `voided_at` on every original (rewriting a sale day the owner may already have reviewed and banked against) **and** posted a full-amount `refund` for each — a double reversal that also mis-typed a deposit collection and a top-up as `refund`. Now the SQL twin of `markCancelled`: originals **untouched**, one dated compensating row appended per reversible original — `sale` → a positive `refund`, `crate_deposit` → a **NEGATIVE `crate_deposit`**, `wallet_topup` → a **NEGATIVE `wallet_topup`** (#190's in-family rule; a refund would move Cash refunds for money that was never in Cash sales) — each copying the original's method and store, and rows of any other type never re-reversed. The `voided_payments` response key is retained but always empty (wire compatibility). The WALLET arm gets the same treatment, which closes the "R2" hold at the client flag check: every original leg is reversed by an appended opposite (goods debit → `refund` credit, payment credit → `void` debit, held deposit → `crate_deposit_refunded` debit, NOT the generic `void` which lands in the spendable bucket — #162). It also restores each line's **cost layer** at the COGS the sale snapshotted: that became load-bearing when **0167 (#187)** removed the recost's `qty_remaining` write-back, so nothing else would bring those units' cost back on this path. **(3) The web adjustment RPC wrote no cost at all** — `_apply_stock_adjustment` (0141) inserted `stock_adjustments` with no `value_kobo`/`unit_cost_kobo` and touched no `cost_batches`, while 0140 already batched receive/add-product. Now a DECREASE draws the queue oldest-first through **`public.fifo_assign` (0133)** — the same pure function the recost replays through, so there is one FIFO algorithm on the server — and snapshots what it drew; an INCREASE mints one fresh layer down the same three-step ladder the mobile approval walks: the cost the REQUEST stated (**#197's `stock_adjustment_requests.unit_cost_kobo`, cloud 0168** — #197 left the web arm to this issue), else the product's recorded scalar (#189), else Uncosted 0. The fallback is keyed on **NULL, never on a handed-over 0**, so a deliberate "no cost" still gets its Uncosted layer. Both new arms are INCREMENTAL drawers (read `qty_remaining`, replay nothing), so 0167's ownership rule holds unchanged even though its count of drawers is now out of date. **THE OVERLOAD TRAP, hit twice**: `_apply_stock_adjustment` and `request_stock_adjustment` each take a new trailing `p_unit_cost_kobo bigint DEFAULT NULL`, so each is preceded by an explicit `DROP FUNCTION` of its OLD signature — `CREATE OR REPLACE` with an added parameter **overloads instead of replacing**, and PostgREST then answers PGRST203 on every call. For `_apply_stock_adjustment` (REVOKEd from `public`, never a PostgREST surface) the failure would have been *worse than an error*: silent, with 6-arg calls binding to the stale cost-less definition. Dropping a function two PL/pgSQL callers reference is safe — their bodies are opaque strings resolved at run time, so Postgres records no dependency. **DEPLOY ORDER: 0170 requires 0168 (the column part 3 reads — without it the new bodies do not compile), 0167, 0164, 0163, 0155, 0141 and 0133; 0169 (#202) is independent but must not be undone, since none of the four types written here is `purchase`.** Relative to the app 0170 may be pushed **in either order with any client build** — it adds no column and no new request shape, only two response keys (`payment_transactions`, `cost_batches`) that the companion `_applyDomainResponse` handlers read and an older client ignores; an un-deployed 0170 just leaves the v2 flags un-flippable, which is the status quo. **The flags stay OFF** (gated on #121, plus one remaining crate hold: neither this cancel nor the client's early return reverses the customer's issued crates, so do not enable `cancel_order` for a crate business until the Crate Pool seam has a server arm — ADR 0020).

- **2026-07-29** — **#202 the PRD #155 dead-code sweep (three promised removals, all dangerous-not-merely-unused)**: (1) **Legacy customer-payment paths deleted.** `CustomerService.addPayment` / `refundToWallet` / `updateWalletBalance` had **zero callers** and each mutated a customer's credit balance by appending a `wallet_transactions` row with **no matching `payment_transactions` row and `staffId: ''`** — precisely the discipline #155 established (every money movement is one wallet leg AND one payment leg, posted atomically by `CreditLedgerService`). Their shared primitive `CustomersDao.updateWalletBalance` went with them (its own doc named the legacy flows as its only users), so the wallet-only write path no longer exists to be picked up by the next reader — a comment in `daos_customers.dart` says so. Also removed: the whole in-memory `PaymentService` + `paymentServiceProvider` (defined, never consumed — supplier payments live in `SupplierLedgerDao`), both orphaned `Payment` models, and `Customer.payments` (hardcoded `const []` with a "fetch from Payments table" TODO, no reader). (2) **Cart crate-credit block deleted.** `cart_screen.dart` computed a `customerCrateCredit` and **subtracted it from the payable** (`tot = sub − discounts − credit`) plus rendered a "Deposit Paid" line, all read from `Customer.emptyCratesBalance` — hardcoded `const {}` at every construction site, so always ₦0. **Wiring it up as written would have been a money bug, not a feature**: the crate balance sign convention is positive = the customer OWES us empties, so crate **debtors** would have received checkout discounts, keyed fragilely by manufacturer *name* (`CRATE_TRACKING_AUDIT.md` §D). The field itself is gone from the model so the trap cannot be re-armed. Side effect worth knowing: the cart's goods total is now **exactly `subtotal − discounts`**, the same two terms `ReceiptTotals.totalKobo` nets — so checkout's printed total and the receipt block agree **by construction** rather than by both happening to be 0 (the stale "that term is dead" notes in `receipt_totals.dart` / `checkout_page.dart` are updated to say this). (3) **`purchase` payment type dropped from both CHECKs** — Drift **schemaVersion 77** + cloud **`0169_drop_purchase_payment_type.sql`**. It had been in `payment_transactions.type` since 0001 and **nothing has ever written it**: every Dart insert seam passes a literal (`sale`/`crate_deposit`/`wallet_topup`/`expense`/`refund`/`van_remittance`), the only parameterised writer (`postReversalPayment`) is called with `refund`/`expense`/`wallet_topup` or copies the original row's type, and all 17 cloud RPCs that insert a payment row hardcode one of the five server-side types. (The `'purchase'` strings elsewhere in the tree are `stock_transactions`' ref-type discriminator for `purchase_id` — a different table.) A goods purchase is a supplier invoice + an `expense`/supplier-ledger payment; do not resurrect the value for it. **The first NARROWING of this CHECK**, which is the gotcha: every prior edit (#169 `crate_deposit`, #144 `van_remittance`) was a widening that could not fail on existing data, whereas a narrowing rebuild **aborts on a single offending row** — and on the client that means the app bricks on open, while a payment row is append-only money that must never be deleted to make a constraint fit. So **both** halves guard first and **skip** rather than fail: the Drift v77 step probes `WHERE type = 'purchase' LIMIT 1` before `m.alterTable` (`SchemaAudit` only checks missing tables/columns, so a device left on the wider CHECK is harmless), and 0169 counts the rows and `RAISE WARNING`s instead of aborting the deploy. No `columnTransformer` needed in v77 (unlike v64): no column changes, and a DB arriving there has already passed v64/v72, so every current column exists on the table being copied. Deploy order is free either way — a v76 client can only produce the six live types.

- **2026-07-29** — **Deposit releases are an IN-FAMILY reversal, never a `refund` (#190, PRD #155 close-out)**: `markCancelled` has documented the rule since #175 — releasing a held `crate_deposit` posts a **NEGATIVE `crate_deposit`** row, *"NOT a `refund` — that would land in Cash refunds while the collection was never in Cash sales, breaking the symmetry"* — but the two OTHER paths that release the same money ignored it. **Confirm** (`OrdersDao.settleCrateDepositReturn`, cash-refund branch) and **§18.3 Refund Cash** (`CreditLedgerService.refundCash`, deposit portion) both posted a positive `type: 'refund'` row. Consequence: `refundsKobo` is **subtracted from `periodNetResultKobo`** and rendered as "Refunds" on the Sales card, so handing back a ₦2,500 deposit read as a flat ₦2,500 loss **plus** a ₦2,500 sales refund that never happened — for money that was only ever a liability (Total Sales is item-line based and deposit-exclusive, `report_revenue.dart`). Meanwhile "Crate deposits held (cash)" (which sums `crate_deposit` rows) never netted down, so held deposits grew forever. **Why it stayed invisible:** same-period collect-then-return still ties at the DRAWER total — the two errors cancel (`cashCrateDeposits` is outside `cashIn`/`cashOut`, so +2,500 held and −2,500 out net to zero) — but both *lines* are wrong and across periods it does not net at all. Fix takes the issue's preferred option: both paths now post a negative `crate_deposit`, so **`recon_data.dart` needed no change at all** (the held line nets on its own). **Secondary (real money bug):** Confirm hardcoded `method: 'cash'`, so a deposit collected by TRANSFER was refunded out of the drawer; the tender **and** the store are now copied from the order's original `crate_deposit` collection row (`postReversalPayment`'s rule, applied by hand), falling back to the order's store + cash for a legacy pre-#175 order that bundled its deposit into the `sale` row. **Gotcha — why the `postReversalPayment` seam is NOT reused here:** it mints a fresh `UuidV7` (#188 needs this leg's id DETERMINISTIC via `settlementLegId`, or two offline tills refund twice) and it copies the original's `order_id` (which would drop the release into `markCancelled`'s reversal set); the `'cash_refund_payment'` id seed is a fixed literal and deliberately survives the retype. §18.3's credit portion stays a genuine `refund` — it pays back spendable credit, not held money. Regression `test/dashboard/recon_deposit_release_test.dart` pins the REPORT consequence, not the row shape: computing the same day with/without the release, the only figure allowed to move is the held-deposit line — plus a counter-example test that reproduces the old `refund` shape and asserts the phantom refund + phantom loss it produced.
- **2026-07-29** — **#195 Profit report store-scoped + ONE Total Sales definition really shared (US 28)**: #176 created `computeTotalSalesKobo` but only two screens called it, so the "one answer" claim was never enforced. Four fixes. (1) **The Profit report was not store-scoped at all** — it read `allOrdersProvider` and passed only `inSpan`, so with a store locked Home and Recon showed the store while Profit showed the whole business. It now takes `reconStoreFilter(ref)` — the SAME predicate the reconciliation uses, so it also inherits the non-CEO confinement and the #140 van exclusion — and applies it **per LINE** in the costed loop (the order-level `vans.isVan` skip is gone; a van fails `reconStoreFilter` by construction). (2) **The reconciliation now consumes the helper**: `ReconData.totalSalesKobo` is a stored field fed by `computeTotalSalesKobo(orders, inSpan:, inScope:)` instead of the getter's own `totalRevenueKobo − discountsKobo`, which was equal only "by construction" with nothing enforcing it; the gross/discount pair stays for the P&L split, and a ReconData fabricated without the field still falls back to that identity so the getter-only test harnesses keep working. (3) **Home now scopes per LINE like Recon** (`reconStoreFilter`, not `order.storeId == _selectedStoreId`), keeping a coarse order-level pre-filter (any in-scope line, or the order's own store, which carries the discount) so the drill-down list still matches. (4) **The drill-down no longer contradicts the tile**: `SalesDetailScreen` takes the tile's `inScope` and subtracts the orders' discounts from the header (both modes — a discount reduces revenue AND profit), with a "Less discounts" chip and a TOTAL row in the CSV so the per-item rows visibly add up. Four deposit-inclusive stragglers converted to the one basis via a new per-order helper **`orderGoodsNetKobo`** (in-scope lines − scoped discount, deposit-exclusive), which `computeTotalSalesKobo` now delegates to: Home's staff league table (was Σ`totalAmountKobo`, so a cashier who took big deposits outranked one who sold more goods), Home's Net Profit tile (now nets discounts + scopes per line — still goods-margin-less-expenses, deliberately NOT recon's full `netProfitKobo`), `OrdersDao.getSalesTotalsForStaff` (was `net_amount_kobo`; now item lines − discounts as **two queries, not a join** — joining repeats each order's `discount_kobo` per line and would over-subtract it), and the Profile "Sales Volume" stat (which also stopped raw-selecting `orders` with no `business_id` — invariant #5). **Structural change worth knowing: `computeReconData` is split into a gather half and a pure half.** New `ReconInputs` (every row set + settings + span + `inScope`, all optional-defaulted) and `ReconData reconDataFrom(ReconInputs)`; `computeReconData(ref, …)` is now only the `ref.watch` gather that builds `ReconInputs` and delegates. Crate reads stay behind the `showCrates` gate so a non-crate business still opens no crate stream. That split is what made the AC testable — the old #176 test fabricated a `ReconData` from figures it computed itself and never ran the roll-up, so it could not fail; the rewritten `test/dashboard/report_revenue_test.dart` runs `reconDataFrom` for real and every mutation tried (recon's loop dropping the discount's store scope, recon's loop dropping the per-LINE store scope, the helper becoming deposit-inclusive) turns it red — the load-bearing assertion being `totalRevenueKobo − discountsKobo == totalSalesKobo` at BOTH All-Stores and a locked store, since the two sides reach it by independent loops. **`ReconData.totalSalesKobo` is a REQUIRED constructor field**, deliberately unlike the optional-defaulted additive fields around it: an optional one would let a future construction site quietly fall back to the derivation this issue deleted. Two helpers now own the rules that used to be re-decided per screen — `orderGoodsNetKobo` (one order's Total-Sales share) and `orderDiscountKobo` (which store wears an order-level discount) — and recon's own paid-now/on-credit split reads the former rather than recomputing it. Forfeit income stays OUT of Home's Net Profit on purpose: wallet rows carry no store, so it is business-wide, and adding it to a store-scoped tile would re-open the cross-scope contradiction this issue closes.
- **2026-07-29** — **#197 (PRD #155 US 22) a stock-adjustment request records what the goods cost**: `stock_adjustment_requests` gains ONE nullable column, `unit_cost_kobo` — Drift **schemaVersion 76** + cloud **`0168_stock_request_unit_cost.sql`** (`bigint`, never `integer`: an int4 `_kobo` column caps at ₦21,474,836.47 and is rejected on push with `22003 out of range`, jamming that device's outbox — the 0130 lesson). Until now there was nowhere to put a cost, so `StockAdjustmentRequestsDao.approveRequest` called `adjustStock` with no `inflowUnitCostKobo` and the FIFO batch it minted was priced at 0 (#189 later softened that to the product's recorded scalar price — sane, but still an inference). **The person filing the request is the one holding the invoice**, so the cost is captured on the request row and threaded into the approval's inflow batch, giving a three-step cost ladder: (1) the cost the REQUEST captured always wins; (2) else the product's recorded price (#189); (3) Uncosted 0 only when neither is on file. **NULL is load-bearing, not laziness** — a recount that simply found more units has no invoice behind it, and NULL is exactly what keeps #189's fallback running instead of asserting a made-up 0; every pre-existing row is correctly NULL and needs no repair. **Only an INCREASE carries one**: `requestStockAdjustment` writes `Value(quantityDiff > 0 ? unitCostKobo : null)`, because a decrease is valued by drawing the store's FIFO queue at approval time and snapshotting what it drew (#7a) — the requester is not the authority on what a loss cost. Deliberately **no CHECK** enforcing that in the cloud: the rule lives with the cost logic, and a CHECK would reject an otherwise-valid legacy row on a re-push over a value that is simply ignored. UI: an optional "Cost per unit" field on the Update Stock sheet (add mode only, hinting the recorded price and saying out loud what leaving it blank will do), threaded into BOTH arms — the stock keeper's `requestStockAdjustment` and the Manager/CEO's direct `adjustStock`; the approvals card shows cost-per-unit + total cost so the approver can reject a wrong price instead of discovering it later as a wrong profit figure, and renders "Not stated — will use the recorded price" when there is none. **A typed `0` reads as "not stated"**: the field offers no way to say "these goods were genuinely free", so honouring it as the deliberate Uncosted 0 that `adjustStock` treats an explicit 0 as would turn a slip of the finger into units that sell at 0 COGS forever. `stock_adjustment_requests` has no `pushColumns` whitelist entry (pass-through) and `pos_pull_snapshot` serialises with `to_jsonb(t)`, so the column syncs both ways with **no registry or RPC change**. The web arm (`request_stock_adjustment` / `approve_stock_adjustment`, 0141) has no cost parameter and stays out of scope (flagged to the web repo) — its golden scenarios are `dart_arm_only`. **DEPLOY ORDER: push 0168 BEFORE a v76 app reaches a device**, or the `stock_adjustment_requests` upserts carrying `unit_cost_kobo` are rejected cloud-side (PGRST204, column not in the schema cache).

- **2026-07-29** — **`CreditLedgerService` payment rows now stamp `store_id` (#194, PRD #155 US 36)**: the `wallet_topup` row in `topup` (Add Credit / repayment collection) and `postCashRow` in `refundCash` (the §18.3 cash-out `refund` row) were the last two live `payment_transactions` insert sites writing NULL into `store_id`. **The refund one was a real money-visibility bug**: the Sales card's Refunds figure filters on the row's store (`refundsKobo` loop → `inScope(p.storeId)`) and `reconStoreFilter` (`recon_data.dart:98`) returns **false for a null store whenever a store is locked** — so a customer cash refund vanished from the Sales card under a locked store while still showing under All Stores. (The `wallet_topup` NULLs were harmless only by luck: the cash card does not filter store.) Fix threads an optional `storeId` down `CreditLedgerService.topup`/`refundCash` → `CustomerService.topUpWallet`/`refundCashFromWallet` → the two Customer Details sheets, which resolve it from **`activeWriteStoreProvider`** — locked store, else first selectable, else home store. That provider (not bare `lockedStoreProvider`) is the right source for a money write: it never yields "All Stores", it is the same resolution expenses §20.8 / supplier activity §21.11 / a POS sale already use, and `selectableStoresProvider` keeps vans out of it — a van-stamped row would be dropped by `reconStoreFilter` outright. Null is still accepted and reports business-wide, as legacy rows do. **Gotcha for the next slice:** only the v1 (flag-OFF) path keeps the stamp. `pos_wallet_topup` takes no store parameter AND the sync service restores the row the server returns back over the pre-inserted local one (`_restoreTableData`, domain-RPC response branch) — so with `feature.domain_rpcs_v2.wallet_topup` ON the store is lost **locally too**, not just in the cloud. Closing it needs a migration adding `p_store_id` **plus** dropping the old signature ([[project_rpc_param_add_overload_trap]]) — worth filing before that flag is ever flipped. Regression `test/wallet/credit_ledger_store_stamp_test.dart` pins the stamp on the local row **and** the pushed payload for both paths — a store that exists only locally converges nowhere.
- **2026-07-27** — **Widget tests that assert on scrollable lists must pin a phone-sized surface**: `who_is_working_screen_test.dart` failed on `find.text('Carol')` (3rd of 3 staff) while Alice/Bob passed — it looked exactly like a data-filter bug but was pure layout. The default `flutter_test` surface is **800×600 logical**, and `responsive.dart`'s `_scaleFactor` keys off **WIDTH ONLY** (`screenWidth / 375`, clamped 0.8–1.5): 800px wide pegs the scale at the **1.5× cap**, inflating the picker header to 277px — while the surface is still only 600px tall. That leaves the staff `ListView` a **205px viewport (~1.5 cards)**, so card 3 lays out at y=576, below the fold. It IS built (250px default `cacheExtent`) but is **offstage**, and `find.text` defaults to `skipOffstage: true` — so the finder returns 0. **The failure message is byte-identical to a genuinely-missing row**, which is what makes this class of bug read as a query/filter problem: verified by mutating Carol to `suspended` and getting the same `Found 0 widgets with text "Carol"`. Fix = pin a real phone surface in the pump helper (`tester.view.physicalSize = Size(1125, 2436)`, `devicePixelRatio = 3.0` → 375×812 logical, scale 1.0, ~548px viewport, 6 cards fit) + `addTearDown(tester.view.resetPhysicalSize/resetDevicePixelRatio)`. Product code is CORRECT — real devices never hit this; the 800×600 default is the worst case of wide-screen chrome scaling on a phone-height screen. **Diagnostic seam worth reusing:** split the probe into a plain `test()` for the DAO (real async) + a `testWidgets()` for the render, and dump `find.byType(Text, skipOffstage: false)` + `tester.getRect` of the Scrollable — that separates "data layer dropped it" from "laid out below the fold" in one run. **Gotchas hit while probing:** awaiting a Drift stream's `.future` inside `testWidgets` **deadlocks** (fake-async zone never pumps the real DB) — read the DAO from a plain `test()` instead; and macOS has no `timeout` binary — use `flutter test --timeout 45s`. Latent repo-wide: 24 of 27 `pumpWidget` test files pin no view size.
- **2026-07-25** — **PRD #155 close-out audit — 38 stories verified, 20 follow-ups filed (#187–#206), umbrella closed**: seven parallel agents audited every user story against the shipped code, one domain each (compensating rows / tender+cash card / Confirm safety / day close / cost-batch coverage / report truth / cross-cutting), with every headline finding independently re-verified against source, SQL and prod. Baseline healthy: `flutter analyze` clean, **745 tests pass**, migration ledger pristine (144 local files = 144 rows, all Local+Remote aligned; 0159 correctly absent, parked on the FCM branch). Outcome: **23 stories done, 12 partial, 3 never implemented** — closing all 8 slices was not the same as delivering the PRD. **Biggest find (#187, P0):** `pos_recost_product_store` (`0133_fifo_batch_consumption.sql:207-267`) replays the FIFO queue from `qty_original` against **sale lines only** and then writes derived remainders back over `cost_batches.qty_remaining`. It was authored when sales were the only batch consumer; #170 then added client-side draws for damages, shortages, count corrections, transfer dispatches and delete write-offs. Since it fires on every sale *and* cancel push and `cost_batches` is pass-through synced, **every non-sale draw is resurrected** — a damaged unit is booked as a valued loss *and* later sold at real COGS, transfer source stores regain the phantom coverage 7b removed, and a cancel restores units twice (B1 qty_original 10, sell 4, cancel ⇒ B1 back to 10 + new B2(4) = 14 covered against 10 on hand). This makes `daos_orders.dart:1378-1383`'s "the authoritative replay supersedes this layer" comment false against the shipped SQL. **Prod data not touched** — blast radius unquantified, repair pass deferred pending sign-off. Other live defects: offline double-Confirm still double-refunds a deposit (#188 — only the status re-read landed, not the per-(order,manufacturer) skip; legs use fresh `UuidV7` so the PK upsert can't collapse them, and the existing "two devices" test runs sequentially on one converged DB so it *cannot* fail); three increase paths minting cost-0 batches unreachable by #41's backfill (#189); deposit releases typed `refund` at Confirm/§18.3 against `markCancelled`'s own stated rule (#190); `daily_closings` freezing to the first opener's store lock, already visible in prod (#191). **Gotcha worth remembering:** `gh issue create` silently produced no output and created nothing when run with the scratchpad as cwd — it needs the repo dir or an explicit `--repo`. Docs corrected: ADR 0021 had two stale pre-merge facts (claimed `schemaVersion → 70` and `0160_money_integrity_daily_closings.sql`; actually v68 / app 69 / `0157`) and gained a §4 recording the four settled product decisions (VAT inclusive, forfeit-as-income, snapshot-at-write loss valuation, Confirm at Cashier+) that previously lived only in the progress tracker; ADR 0014's "deferred" day-close alternative now points at #174 and carries a loss-valuation-basis addendum; `CONTEXT.md` gained the Money & Payments section it never had; the tracker gained the missing #172 section. Code-level hygiene (#202 dead-code sweep, #206 golden fixtures) left for its own session per the one-issue-per-session rule.
- **2026-07-25** — **#183 cloud sale RPCs snapshot order_items.catalogue_price_kobo (web/v2 parity with #176) — deployed (0160)**: #176 added the column and the Flutter checkout snapshots the tier list price on a custom-price concession, but no cloud RPC set it, so RPC-path sales (web `checkout_order`, mobile v2) left it NULL — breaking report-truth/margin parity with the Dart path. Migration `0160_money_integrity_rpc_catalogue_price.sql` adds an `IMMUTABLE` helper `_catalogue_price_snapshot(catalogue, charged)` (SQL twin of the Flutter rule: return catalogue only when it is non-null AND differs from the charged price; else NULL — and NULL for product-less lines) and `CREATE OR REPLACE`s `_checkout_insert_lines` (the sole web-checkout line-insert site; `checkout_order` calls it by name and needed no change) + `pos_record_sale_v2` (flag-off v2 path, patched ahead of the flag as the 0091 precedent did), each a VERBATIM copy of its current body with the single delta of inserting `catalogue_price_kobo` from an OPTIONAL `p_items.catalogue_price_kobo` key. No parameter changed → plain `CREATE OR REPLACE`, no overload/DROP/PGRST203; `SECURITY DEFINER`+`search_path` and GRANTs preserved. Client `daos_orders.dart` v2 `thinItems` forwards the optional key (mirroring `buying_price_kobo`/`price_snapshot`); web omits it → NULL (correct — no web-client contract change). Deliberately rejected a product-tier-column heuristic: products carry two tier prices (retailer/wholesaler), so the tier a concession deviated from is not reconstructable server-side from a charged price alone — the exact forwarded key is correct, guessing would fabricate a wrong concession. Independently `/code-review`ed — the verbatim-copy check confirmed the ONLY delta vs the current `main` bodies is the catalogue insert (no FIFO/crate/credit/store_id/confirm logic reverted). PR #185 merged (`fae9ab3`); 0160 applied onto the reconciled ledger (dry-run showed only 0160; `_catalogue_price_snapshot` + both RPCs verified referencing the column). The Tier-2 golden RPC arm (`test/integration/rpcs/checkout_order_golden_test.dart`; needs live DB + fresh `TEST_USER_REFRESH_TOKEN`) exercises the RPC checkout path — recommended as post-deploy confirmation. **Correction (2026-07-25, #155 close-out audit): it does NOT pin catalogue-price parity.** No golden fixture asserts `catalogue_price_kobo` at all (`rg -c catalogue_price test/golden/fixtures/` → 0) because every existing scenario uses a full-price cart, so the column is NULL on both arms and nothing is compared. A concession scenario is needed before this rule is pinned — filed as #206. **#206 landed that scenario (2026-07-29):** `custom-price concession (#176/#183)` charges ₦800 against a ₦1,000 tier price *alongside* a full-price line in the same cart, so both halves are asserted — a real concession is recorded, a full-price line records none. It is GREEN on the Dart arm; the Tier-2 RPC arm now forwards `catalogue_price_kobo` and reads the column back, but **was not executed** in that session (it needs a live DB + a fresh `TEST_USER_REFRESH_TOKEN`), so the rule is pinned in the shared fixture and PROVEN on one arm, not two. Filed **#186** (variance-card money-vs-units basis divergence — likely a pre-existing units under-count exposed, not a money regression).
- **2026-07-25** — **#182 count-shortage loss valued at the #170 write-time snapshot (audit #30, #170 deferral)**: the Daily Reconciliation's count-SHORTAGE figure was the last loss surface still recomputed at *current* cost at report time (`recon_data.dart` shortage loop: `units × products.buying_price_kobo`) — a later cost edit silently restated every past period's shortage/variance/net-result (the failure mode ADR 0005 rejected for COGS; Damages was already fixed in #170). Fix is APP-ONLY (no migration — `value_kobo` already exists, cloud 0155 / Drift v66): a stock count applies each line through `InventoryDao.adjustStock('Daily stock count adjustment', …)` which draws the FIFO queue down and SNAPSHOTS `stock_adjustments.value_kobo`, so the new pure reducer `countShortageLossKobo(adjustments, …)` sums those snapshots over count-reconciliation removals (`isCountReconciliationReason && quantityDiff<0`, in span+scope) via `lossValueKobo` — mirroring the Damages loop, with the same current-cost fallback for legacy pre-#170 rows. `shortageUnits`/`shortageCount`/`ReconShortLine`/retail stay count-session-sourced; only the money basis moved (removed the `shortageCostKobo += units × currentCost` line + its `var`, now a `final` from the reducer). **Scoped out (by design, noted):** SURPLUS stays current-cost (a gain draws no batch, has no snapshot); the ADR 0014 stock-flow-equation card (`stockDamagesKobo`/`stockExpiredKobo`/`stockCountAdjustmentsKobo`) stays current-cost so its closing identity ties out; expiry-via-Record-Damages is already snapshot-valued through the Damages loop (`isDamageReason` matches "expired"); the v2 domain-RPC inventory path (flag off) mints the row server-side — `lossValueKobo`'s null-fallback covers it. Regression test `test/dashboard/recon_shortage_snapshot_test.dart` proves the shortage value is UNCHANGED after a post-count cost edit (30k stays 30k, not 3×99k) + legacy-fallback + disjoint-from-damages/surplus + deleted-product-keeps-value. `flutter analyze` clean; dashboard+costing+inventory suites 212 pass.
- **2026-07-25** — **Money PRD #155 DEPLOYED + supabase migration-ledger divergence reconciled**: cloud migrations **0153–0158** applied to prod (`store_id`, `confirmed_by`, `stock_adjustments.unit_cost_kobo`/`value_kobo`, `stock_transfers.cost_kobo`, `daily_closings`, `order_items.catalogue_price_kobo`); Drift schema v69; #170/#174/#176 closed (umbrella #155 left open). **Gotcha — reconciling a timestamp-diverged ledger:** prod had applied local `0140,0141,0143–0152` under **timestamp** `schema_migrations.version` values (not their 4-digit names), plus the FCM branch's push migration under `20260713151317`, so `supabase db push` saw all of them as *pending* and would have re-run already-applied migrations. Fix is ledger-only (schema untouched): `supabase migration repair --status applied 0140 0141 0143 0144 0145 0146 0147 0148 0149 0150 0151 0152`, then `supabase migration repair --status reverted <the 13 timestamp rows>`, verify with `supabase db push --dry-run` (must list ONLY the intended set), then `db push`. CLI v2.95.4 connects non-interactively (`migration list --linked` exits 0). Push-notifications (#138) **parked, not lost**: its migration was renumbered `0153_push_notifications.sql` → `0159_push_notifications.sql` on `feat/push-notifications-fcm` and made idempotent; prod already physically has `devices.fcm_token`, so at merge rely on the `if not exists` guards or `migration repair --status applied 0159`. Post-deploy advisors: 0 ERROR, 109 WARN (all pre-existing `function_search_path_mutable`). App is now safe to release to devices — the outbox will not jam on unknown columns.
- **2026-07-24** — **#163 net-position honesty: subtract held deposits + supplier crate debt (ADR 0020)**: `ReconData.businessNetPositionKobo` (Daily Reconciliation "Business worth right now") already booked the physical empties as an ASSET (`crateDepositKobo`) but ignored the matching crate LIABILITIES, overstating worth; it now also subtracts (1) the crate deposits we HOLD for customers (reuses `crateDepositSummaryProvider.heldKobo` — the `crate_deposit` wallet-family net taken−refunded−forfeited) and (2) the crate debt we owe SUPPLIERS — a NEW business-wide derived read `CratePoolDao.watchSupplierCrateDebtValueKobo()` = `SUM(quantity_delta)` per manufacturer across EVERY supplier × current `depositAmountKobo` (the all-suppliers roll-up of #160's `watchSupplierCrateDebt`, same current-rate join), via `supplierCrateDebtValueKoboProvider`. Getter: `inventoryOnHand + customerDebt + crateAsset − supplierPayable − heldCustomerDeposits − supplierCrateDebt`; both crate legs valued at the SAME current deposit rate as the asset (net symmetrically) and gated on `showCrates` (0 for a non-crate business — no deposit-family rows, no `supplier_crate_ledger`). Detail card + CSV export gain the two subtracted lines (card lines shown only when non-zero). Pure read + getter — NO migration, NO writes, append-only ledgers untouched; trustworthy only because #158–#162 made the balances ledger-derived. A supplier crate CREDIT (negative balance) correctly nets the debt down.
- **2026-07-24** — **#162 Cancel completes the crate ledger (ADR 0020)**: `OrdersDao.markCancelled` (v1 path) now (1) reverses the crate rows a sale ISSUED to the customer via a new seam verb `CratePoolDao.reverseIssuedByCustomer` — the ENQUEUED twin of `reverseIssuedByCustomerLocal` (shared private `_reverseIssuedByCustomer({enqueue})`); a cancel reverses a sale the cloud ACCEPTED, so the compensating `-qty` 'adjusted' `crate_ledger` row SYNCS (the rejected-sale reversal stays local-only), and the customer's derived crate debt nets back to pre-sale — no phantom debt; the `customer_crate_balances` cache is decremented locally but never enqueued (#158 projection); and (2) releases the held `crate_deposit` wallet leg with a DEPOSIT-FAMILY `crate_deposit_refunded` debit, NOT the generic `'void'` — `'void'` is outside `kCrateDepositReferenceTypes` so it lands in the SPENDABLE sum (wrongly docking spendable) and never offsets the `crate_deposit` credit (leaving "deposits held" inflated); the deposit-family debit deflates held to 0 and is excluded from spendable. The reversal filter also skips legs that are THEMSELVES reversals/settlements (refund/void + deposit-family debits + spendable `crate_refund`) so only ORIGINAL sale legs are reversed, never a compensation. Crate write stays inside the seam (crate_seam_ban_test). No migration. Scope = the Checkout→Cancel path (an unsettled sale); a full reversal of a deposit already SETTLED at Confirm is deferred (PRD #156 A3/A6, late-refund window) — `markCancelled` has no status guard, so cancelling an already-settled order would over-release. The v2 `pos_cancel_order` path is untouched (flag held off until it mints the reversal).
- **2026-07-24** — **#166 demote `manufacturer_crate_balances` off the push set (ADR 0020, closes the last leak)**: the business-wide per-manufacturer physical-pool cache was the ONE crate balance still pushed absolute; its read already derived from the ledger (#159), so this slice only demotes the write/push — `CratePoolDao.recordCrateReturnByManufacturer` (its SOLE enqueue site) no longer re-reads the balance and `enqueueUpsert`s it, and `'manufacturer_crate_balances'` is removed from `_tablePushPriority` (it was never in `_naturalKeyPushConflictTargets` and had no `_backfillTable`, so nothing else pushed it); local `customInsert` cache write kept (Crates tab reads it); `SyncedTable` isCache restore-on-pull entry retained (legacy/web/RPC cloud rows still restore into the now-unread projection); golden cache-membership set unchanged (same as `customer_crate_balances` after #158). **After this slice NO crate balance is pushed at all** — only `crate_ledger`/`supplier_crate_ledger` sync; ADR 0020's hard contract holds for all four caches + the `empty_crate_stock` scalar. No migration.
- **2026-07-23** — **#160 supplier crate debt derived + Receive Stock posts both legs (ADR 0020)**: `CratePoolDao.watchSupplierCrateDebt` sums the supplier's `supplier_crate_ledger` rows per manufacturer (wallet-style, forwarded from `SupplierCrateBalancesDao.watchBySupplier`); `supplier_crate_balances` demoted to a LOCAL-ONLY projection (still written, no longer enqueued, off the push-priority + natural-key maps); **Receive Stock posts BOTH legs in one transaction through the seam** — the physical `crate_ledger` movement AND the `supplier_crate_ledger` movement for empties returned on a delivery — so the yard count and supplier balance can't disagree and the stock keeper never re-enters the return on the supplier screen; no migration (v63 seed already made ledger sum == cache); `manufacturer_crate_balances` is now the SOLE remaining crate cache still pushed absolute (follow-up).
- **2026-07-23** — **#159 physical empties pool derived from the ledger (ADR 0020)**: `CratePoolDao.watchEmptiesPoolByManufacturer({storeId})` sums the physical-pool `crate_ledger` rows (`store_id IS NOT NULL AND customer_id IS NULL`) grouped by manufacturer (storeId null = business-wide, so business total == Σ store totals by construction); `manufacturers.empty_crate_stock` (via a new `manufacturers` push-column whitelist that excludes it) + `store_crate_balances` are demoted to LOCAL-ONLY projections — only `crate_ledger` syncs the pool, killing the counter-only-grows asymmetry + cross-store clamp drift; dead `verifyCrateReconciliation` deleted; no migration (v63 seed already made SUM(store-stamped) == store caches); #160 follow-ups: manufacturer/supplier crate balances still pushed absolute.
- **2026-07-23** — **#158 customer crate debt derived from ledger**: `CratePoolDao.watchCustomerCrateDebt` sums `crate_ledger` per manufacturer (mirroring the wallet); `customer_crate_balances` is demoted to a LOCAL-ONLY projection (still written, no longer enqueued) so two offline tills' movements both survive a merge; no migration (v63 seed already made ledger sum == cache at cutover).
- **2026-07-22** — **#157 CratePoolDao seam + complete ledger (ADR 0020)**: `CratePoolDao` (daos_crates.dart) is the SOLE writer of the six crate tables + `manufacturers.empty_crate_stock` scalar; `crate_seam_ban_test` fails the build on any write outside it (raw-SQL AND `ManufacturersCompanion` builder forms); Drift v62→v63 `_seedCrateOpeningLedger` appends opening rows LOCAL-ONLY (migration writes never enqueue); manufacturer pool not yet unified (#159).
- **2026-07-21** — **POS grid coaching banners merged (copy contract)**: the two POS grid hints are now one banner (`_posGridHintMessage`); POS long-press opens the quantity/discount sheet ("choose a quantity") — it does NOT edit the product; only Inventory's hold-to-edit banner (#110) really edits; keys `hintPosTapAdd`/`hintPosLongpress`, retire-after-2 intact.
- **2026-07-21** — **#112 only three selectable industries**: a `selectable` flag on industry.dart exposes exactly Beverage distributor, Pharmacy, Frozen Foods & Grocery (`selectableCatalogue`); the other six stay in the registry (no migration) so hidden-trade tenants keep normalizing/features and are grandfathered in Settings; "Beverage distributor" label maps to legacy DB `'Beer distributor'`.
- **2026-07-21** — **#109 category rename/delete sync-correctness**: `softDeleteCategoryAndReassign` pushes an EXPLICIT null `category_id` (copyWith, not `Value.absent`) so the cloud actually clears it; deleted-category products fall into the `kUncategorizedCategoryId` sentinel bucket; rename collision guard is case-insensitive-excluding-self; reuses `Gates.addProduct`.
- **2026-07-20** — **#108 optional product unit / "No unit"**: `products.unit` is nullable (absent renders nothing, null == not-a-bottle); the unit selector moved into the always-visible Add Product form and defaults to null ("No unit") — which REMOVES the #80 auto-Bottle default that engages crate tracking for beverage tenants.
- **2026-07-20** — **#115 stuck approvals count root cause**: the count froze because the PULL path resurrected resolved rows — the status-blind LWW clobber-guard let a same-second/clock-skewed cloud `pending` overwrite a locally-resolved request; fix = `Restore.monotonicStatus` on both feeders (`stock_adjustment_requests` + `quick_sale_requests`), dropping an incoming `pending` whenever the local row is already terminal.
- **2026-07-20** — **#153 ChangeNotifierProvider disposes service-owned notifiers**: `ChangeNotifierProvider` disposes whatever notifier its body returns (on rebuild AND teardown), so never return a service-owned one — use non-owning `mirrorNotifier<T>` (core/providers/mirror_notifier.dart); `mirror_notifier_ban_test` fails the build if any `ChangeNotifierProvider` body returns a borrowed `ref.watch`/`ref.read` notifier.
- **2026-07-20** — **#113 product barcode soft-uniqueness**: `products.barcode` is nullable text with NO DB UNIQUE constraint (a hard UNIQUE would 23505-reject one of two offline tills and jam the outbox), so a barcode may map to several products; the "already used by X" warning never blocks Save; `catalogDao.findProductByBarcode` returns the first match.
- **2026-07-20** — **#117 self-resign + admin-removed offboarding**: staffer "Leave my account", owner-sees-"Delete Business", and device auto-logout on membership→`removed` all route through `AuthService.logOutCurrentUser` (local-only wipe, gated to sole-member-on-device); `clearAllData`/`fullLogout` never touch the cloud, so a restored membership re-syncs.
- **2026-07-20** — **#107 staff Remove (offboard)**: RPC `remove_staff_member(p_business_id,p_user_id)` (migration 0149) called DIRECT (not via outbox, like `delete_business`) sets `user_businesses.status='removed'` + nulls `users.auth_user_id` (frees the email) while KEEPING the users row as an Attribution Stub; gated on `Gates.staffRemove` (`staff.remove`, CEO-only); owner + self removal rejected both sides.
- **2026-07-20/21** — **#106 QA batch verified (pure passes)**: #116=receipt paper size 58/80mm (32/48 chars, default 58mm; device-local SharedPreferences key `printer_paper_size`, never synced); #114=invisible ListTile tap-ripple fixed via a transparent `Material(type: transparency)` above the coloured wrapper (the cart-screen pattern); #111=POS search bar always visible, app-bar search icon/toggle retired; #110=Inventory "press and hold to edit" hint, per-staff key `hint_inventory_longpress_<userId>`, gated `Gates.editProductPrice`, retires permanently in one tap; #119=Home Reports shows an attention dot not a number (CEO/Manager-only, cleared by opening Daily Reconciliation via `ReconReviewMarkerNotifier`, per-user); #118=POS "Scan" AppFAB always visible, behind the `barcodeScannerProvider` seam, `scanOnce`, unknown→`AddProductScreen(prefilledBarcode:)`, shared barcode resolves to first match; #102=30s periodic catch-up pull observed converging within one tick; #101=Broadcast verified-by-evidence only (emulator can't hold realtime channels — broadcast + `postgres_changes` share one websocket and fail together, HTTPS periodic pull unaffected).
- **2026-07-19** — **analyzer must exclude build/**: the Dart analyzer ignores `.gitignore`, so generated `build/` output (e.g. stray `firebase_messaging` copies under `build/{ios,macos}/SourcePackages`) gets analyzed and throws phantom errors — analysis_options.yaml now sets `analyzer.exclude: [build/**]`.
- **2026-07-19** — **#150 Sync Issues cancel-rejected-sale route**: the Sync Issues orphan tile for a `domain:pos_record_sale_v2` row now offers "Cancel this sale" (`OrderCommands.cancelRejectedSaleFromOrphan`) as a second recovery route; critical ordering — run the reversal BEFORE `discardOrphan`, since the reversal sources sold lines from the orphan's `p_items`.
- **2026-07-19** — **#149 rejected v2 sale no longer leaks a phantom cloud order**: all order-header pushes route through the sole envelope-aware `OrdersDao._enqueueFullOrder`, which classifies the `pos_record_sale_v2` envelope (via `SyncDao.saleEnvelopeState`) and SKIPS the header on REJECTED, HOLDS it (`heldByOrderId`, #121 machinery) on PENDING, enqueues normally on CONFIRMED/v1; the only remaining direct `enqueueUpsert('orders')` is v1 `createOrder`.
- **2026-07-11** — **oversell orphan one-tap Cancel + crate reversal**: `OrderCommands.cancelRejectedSale` re-sources sold lines from the orphaned envelope (`SyncDao.rejectedSalePayload` reads `p_items`/`p_store_id`) and runs the full LOCAL reversal (order+inventory+wallet+crate); crate leg via `CrateLedgerDao.reverseIssuedByCustomerLocal` appends a compensating −qty 'adjusted' row + decrements the LWW cache (local-only, never enqueued — a rejected v2 sale never reached the cloud).
- **2026-07-11** — **#102 periodic safety-net pull, guarded**: the 30s periodic `catchUpPull(reason:'periodic')` is the reconnect-replay backstop that makes Broadcast's no-replay acceptable; extracted to `_installPeriodicSafetyNet`/`_periodicSafetyNetTick` and pinned by `periodic_safety_net_test` (30s cadence, idempotent `??=`, cancelled on logout) so it can't be silently removed; cadence decision = KEEP 30s.
- **2026-07-11** — **#101 Broadcast live-signal layer (0147/0148)**: migration 0147 adds a generic `zz_broadcast_sync_signal` AFTER-trigger on ~60 business-scoped tables emitting `{table,id,op}` (no row data) to topic `store_<businessId>` via `realtime.send(private:=true)`; 0148 adds one SELECT-only RLS policy on `realtime.messages` (NO INSERT — clients only receive); client `CloudTransport.startBroadcast` → debounced `catchUpPull('broadcast')`, and Broadcast never writes Drift.
- **2026-07-08** — **#100 crate caches drift under offline races (deferred)**: the four crate balance caches share inventory's push-absolute-LWW shape, so concurrent offline movements to the same owner can drift them — but they're backed by append-only ledgers (`crate_ledger`/`supplier_crate_ledger`) and recomputable via `verifyCrateReconciliation`, which is NOT auto-wired (deferred); `store_crate_balances` `setBalance` is an intentional manual override, not a race. (Superseded 2026-07-23 by #158/#159 deriving these caches from the ledger.)
- **2026-07-08** — **#100 v2 checkout: guarded RPC + client wallet legs**: on the `pos_record_sale_v2` path the §14.3 wallet double-entry is authored client-side (`_postSaleWalletLegs`, before the v1/v2 split) with `p_wallet_amount_kobo=0`, so the guarded RPC owns only the oversell-safe order/items/stock/payment (`SELECT…FOR UPDATE` + relative decrement + `insufficient_stock`/`inventory_row_missing` P0001 + `ON CONFLICT DO NOTHING`); v2 wallet rows are byte-identical to v1's.
- **2026-07-08** — **#100 silent oversell reproduced**: two offline tills selling the last unit both push the ABSOLUTE `inventory.quantity=0` row, which the natural-key LWW cache (`onConflict business_id,product_id,store_id`) collapses to one row while two orders land — 2 sold from stock of 1, no error; `pos_record_sale_v2`'s serialized relative decrement is the fix.
- **2026-07-07** — **#97 realtime socket needs the user JWT at subscribe**: a realtime channel only reads `socket.accessToken` into its join payload at subscribe time, so `startRealtime` now explicitly `realtime.setAuth(currentSession.accessToken)` immediately before `channel.subscribe()` — otherwise `postgres_changes` joins go out on the anon key and are RLS-rejected (REST/pull always worked because the JWT rides each HTTPS request).
- **2026-07-07** — **#95 realtime = one channel, many bindings**: one channel per synced table fired ~55 near-simultaneous `phx_join`s past the realtime per-client ceiling (all `channelError`/`timedOut`); collapse to a single channel with one `postgres_changes` binding per table (the supported realtime_client 2.x many-table pattern).
- **2026-07-07** — **#98 periodic fallback pull implemented**: the documented "periodic fallback poll" never existed (the only 30s `Timer.periodic` drained the outbox); added a foreground+online+business-bound `catchUpPull(reason:'periodic')` to that tick, run before/independent of the push drain, so a console-deleted product stops being sellable within ~one tick regardless of realtime health (realtime = signal, pull = transport).
- **2026-07-07** — **#93 realtime resubscribe teardown race**: `restartRealtimeSync` must AWAIT the full teardown before re-subscribing and flip `_realtimeActive=false` synchronously (re-entrancy guard); `stopRealtime` must snapshot+clear its channel holders synchronously before the awaited removals — else a fire-and-forget teardown leaves the list full, the recreate's `isNotEmpty` guard bails, and zero live channels exist for the whole session.
- **2026-07-07** — **#87 forbid client hard-delete (0145)**: migration 0145 REVOKEs DELETE from `authenticated`+`anon` on all 16 tables carrying an `is_deleted` column, enforcing the soft-delete mandate at the DB layer; SECURITY DEFINER RPCs (`delete_business` cascade, `console_soft_delete_product`) run as owner and are unaffected, and the app's `enqueueDelete` tables are a disjoint set.
- **2026-07-07** — **#88 reconnect/resume catch-up pull + re-stamp (0146)**: new `catchUpPull` (silent, 20s-debounced, pushes outbox first) now fires UNCONDITIONALLY on every offline→online and on app-resume (was "only if last pull failed"); migration 0146 one-time re-stamps `last_updated_at` on soft-deleted rows (positive-sync only — the local "delete rows absent from snapshot" reconcile was explicitly rejected).
- **2026-07-07** — **POS crash on stale category selection**: `firstWhere` on a `selectedCategoryId` that vanished from the stream (renamed/soft-deleted/console-removed) throws `StateError` and crashes POS — `pos_controller._loadCategories` now resets selection to null when the emitted list no longer contains it, plus a `firstWhere`→plain-loop-with-'All'-fallback defense in pos_home_screen.
- **2026-07-07** — **#81 Lexicon morph on POS/inventory/guides**: `Lexicon.itemPlural` (+ item singular/lowercased) is wired into POS grid/coach tips, inventory tab/FAB/search/empty states, and guides; Beverage stays verbatim (`item='Product'`); neutral labels (Suppliers, Price, Stock) stay literal and crate/empties nouns stay `isCrateBusiness`-gated.
- **2026-07-07** — **#80 Lexicon module (industry nouns)**: new lib/core/industry/lexicon.dart holds an industry's domain nouns (item/category/unit) with per-slot generic defaults; `lexiconFor(Industry)` is total and Beverage reproduces today's product-form wording verbatim; surfaced via business-scoped `industryLexiconProvider` so words follow an industry switch with no restart.
- **2026-07-07** — **#79 all nine industries selectable**: added `phoneAndGadgets` + `frozenFoodsAndGrocery` to the `Industry` enum and flipped `comingSoon` off for all (registry-only, no UI change); crate opt-in still gates on `isCrateBusiness` (Bar/Beverage only). Later narrowed back to three by #112.
- **2026-07-07** — **#78 optional synced product photo**: Drift v58→v59 adds nullable `products.image_url`; cloud 0143 adds the column (pass-through push table, no RPC/whitelist change), 0144 creates the public `product-images` bucket + business-scoped storage RLS; `setProductImageUrl` enqueues the FULL product row; photo stays OFF the POS grid/receipts (POS renders `image_path`, #78 lives in `image_url`+local cache). NOTE: the `business-logos` bucket was never created — logo cloud-upload has been a silent no-op.
- **2026-07-06** — **#77 Industry registry + industryOf() (ADR 0015)**: lib/core/industry/industry.dart is the single source of truth for industries; `industryOf(String?)` is a total normalizer (legacy `'Beer distributor'`/any casing → beverage; unknown/null → generic, never throws); `isCrateBusiness` is now a shim = `industryOf(type).crateEligible`; no new column/migration.
- **2026-07-06** — **standardized daily close: no cash balance, opt-in VAT**: cash reconciliation dropped entirely (all cash is deposited; no `daily_closings` table — Hard Rule #8 holds), recon collapsed to 5 CEO / 3 Manager cards; VAT is OFF by default, stored in the synced `settings` table (`vat_enabled` + `vat_rate_bps`, no migration), report-only pass-through — it does NOT touch the P&L profit lines.
- **2026-07-05** — **#72 closing integrity flag (ADR 0014 §④)**: with no cash leg and no stored period-start snapshot, the CEO integrity card surfaces the stock-count variance (physical−expected at cost) as Count-reconciled profit = `netProfit + variance`; a count shortfall is a recording error (unbooked shrinkage/theft), not a separate real loss (no new persistence).
- **2026-07-05** — **#72 closing stock flow-equation (ADR 0014 §①)**: CEO card Opening+Received−COGS−Damages−Expired(±Other)=Expected closing, then Variance=Physical−Expected, all valued at CURRENT per-product cost; opening/closing are reconstructed by rewinding recorded `stock_transactions` deltas from current on-hand, so the equation ties out by construction (stated basis, not faked historical precision).
- **2026-07-05** — **#72 closing cash-flow summary (ADR 0014 §②)**: `payment_transactions` is the unified physical-cash ledger driving the derived cash-movement card (sale/wallet_topup/refund/expense); supplier payments are the one cash-out NOT in it (summed from `supplier_ledger_entries`); expenses taken only from the `expense` payment rows (not also the expenses table) to avoid double-counting; business-wide (no storeId).
- **2026-07-05** — **#70 recon P&L must subtract discounts**: `netRevenueKobo = costedRevenue − discounts`, `grossProfit = netRevenue − COGS`, and margin is measured against NET revenue (`order_items.unitPriceKobo` is gross list price; real payable is `orders.netAmountKobo` / `orders.discountKobo`).
- **2026-07-05** — **currency formatter preserves the caret**: `CurrencyInputFormatter` counts meaningful chars (`[\d.]`, ignoring grouping commas) before the raw caret and re-places it after that same count, so mid-string price edits stay put (truncation clamps to end) instead of jumping to end-of-field.
- **2026-07-05** — **#68 web-pos shared RPC helpers**: web-pos/src/lib/rpc.ts centralizes `newId()`/`friendlyRpcError()`; `toKobo`/`fromKobo` moved into lib/currency.ts — quality-only. (web-pos has since moved to the reebaplus-web repo.)
- **2026-07-05** — **#51 web-pos read-only reports**: `loadSalesReport` reads counted orders (`status in {pending,completed}` = `orderCountsAsSale`) and computes profit over COSTED lines only (a line with no product or `buying_price_kobo<=0` is excluded/Uncosted); profit/COGS tiles gated on `reports.see_profit`, screen on `reports.see_sales`.
- **2026-07-05** — **#49 web-pos realtime**: `useRealtimeRefresh` subscribes one business-scoped channel to `postgres_changes` on products/inventory/cost_batches/orders (filtered `business_id=eq.<id>`) and debounce-re-pulls the RLS-scoped read rather than diffing events; a re-SUBSCRIBED channel and a tab regaining focus/connectivity both re-pull.
- **2026-07-05** — **#50 web-pos stock-adjust + approval gate (0141)**: migration `0141_web_stock_adjustment_rpcs.sql` — `request_stock_adjustment` (CEO/Manager apply immediately, others file pending) + `approve_stock_adjustment`; shared `_apply_stock_adjustment` mirrors mobile `adjustStock` (a correction, no Cost Batch); the adjust dialog renders a store `<select>` for multi-store.
- **2026-07-05** — **#48 web-pos inventory RPCs (0140)**: migration `0140_web_inventory_rpcs.sql` adds `add_product`/`update_product`/`receive_stock` — server-authoritative twins of the mobile Cost Batch producers (one inflow ⇒ one fresh batch, never merged; cost 0 ⇒ uncosted; a cost-0 line never clobbers the scalar; idempotency scoped by `business_id`).
- **2026-07-05** — **#57 web-pos checkout-UI cleanup**: on-screen and `plainText()` receipt both derive from one shared `receiptRows()` so they can't diverge; empties wording unified to "· ₦X deposit".
- **2026-07-05** — **#55 golden discount/debt scenarios**: golden model gained `max_discount_percent` + `clampDiscountKobo` + a `rejected_with` shape; golden `tearDown` must be best-effort `del()` swallowing P0001/23503 because the append-only ledgers can't be deleted (forbid_delete trigger).
- **2026-07-05** — **#53 checkout_order helper extraction (0139)**: migration `0139_checkout_order_helpers.sql` extracts `_checkout_mint_order_number`/`_checkout_insert_lines`/`_checkout_draw_fifo` SECURITY DEFINER helpers, behavior-identical to 0137; numbered 0139 because 0134/0138 are reserved by parked console-admin work.
- **2026-07-05** — **#45 web-pos crate ledger at checkout (0137)**: migration `0137_checkout_order_crate.sql` + `_is_crate_business()` posts empty-crate ledger for registered crate customers; web collects no deposit so every web crate sale is crate-track (`deposit_paid=0`); crate legs never touch inventory/COGS/wallet.
- **2026-07-05** — **#44 web-pos credit & wallet ledger (0136)**: migration `0136_checkout_order_credit.sql` adds `p_customer_id` + credit/wallet methods (dropped the 7-arg overload → PGRST203 trap); append-only wallet legs, balance derived via `_customer_wallet_balance()` excluding the crate-deposit family; `debt_limit_exceeded` enforced server-side.
- **2026-07-05** — **#43 web-pos cash-sale keystone + Golden Suite (0135, ADR 0009)**: migration `0135_checkout_order.sql` = atomic server checkout RPC (FIFO draw under FOR UPDATE, oversell rejected at commit, revenue at `pending`); order number `WEB-NNNNNN-XXXXXX` (WEB- prevents collision with mobile ORD-); helpers `caller_has_permission()` + `caller_max_discount_percent()`; the Golden-Scenario Suite runs identical fixtures against Dart DAO + SQL RPC.
- **2026-07-04** — **#47 web-pos Slice 1 skeleton (ADRs 0007–0012)**: new `web-pos/` Next.js + supabase-js app, online-first (no Drift/outbox), auth = Operator via `profiles.business_id` + `user_businesses` with no custom JWT claims; palettes ported to `palettes.ts`, Vercel root `web-pos/`.
- **2026-07-04** — **#42 batch-on-inflow F6**: `CostBatchesDao.recordInflowBatch` inserts a fresh FIFO layer (never merged) inside the inventory-increment txn from Add Product opening stock and Receive Stock; on the v2 create path the batch is enqueued after the envelope so its FK resolves.
- **2026-07-04** — **#41 cost backfill F5**: `CostBatchesDao.onCostBecameReal` costs every still-uncosted batch on the 0→positive transition (fires once per batch); `applyCostBackfill` restates gap-only lines (`buying_price_kobo==0` re-checked at the row), each line keeps its own period, one `cost.backfill` audit row.
- **2026-07-04** — **#40 COGS correction F4**: the push drain collects touched (product,store) pairs then calls `pos_recost_pairs`; the server rewrite flows back as an ordinary LWW row update; one rolled-up `cost.recosted_on_sync` audit row only when `recosted_count>0`; `flushSale` is deliberately NOT recost-triggered.
- **2026-07-04** — **#38 FIFO draw-down F2**: `drawDownSale` snapshots provisional per-line COGS onto `OrderItems.buyingPriceKobo` in `createOrder`; per-unit rounding `round(total/qty)` matches server `fifo_assign`; NO scalar fallback (uncovered units stay uncosted 0); scalar cache re-points to the oldest COSTED batch, skipping uncosted.
- **2026-07-04** — **#39 server FIFO consumption F3 (0133)**: migration `0133_fifo_batch_consumption.sql` — pure IMMUTABLE `fifo_assign()` + `pos_recost_product_store` + `pos_recost_pairs`, ordered by `orders.created_at`, full replay from `qty_original` ⇒ idempotent and a late earlier-timestamped sale re-costs already-corrected lines; deploy after 0132.
- **2026-07-04** — **#37 Cost Batch schema F1**: Drift v58 `cost_batches` (mutable synced tenant table, `cost_kobo==0`=uncosted), v58 migration seeds one opening batch per (product,store) with a deterministic id via new `UuidV7.deterministic`, one SyncedTable entry, cloud `0132_cost_batches.sql` bigint cost_kobo; `@DriftAccessor` must sit directly on the class or the mixin is dropped.
- **2026-07-04** — **#35 land on POS after onboarding**: deleted the one-shot Add-Product auto-push (`_autoShowAddProductPending` + request/consume) from `navigation_service`/`main_layout`; onboarding now lands on POS with the persona CTA.
- **2026-07-04** — **#34 persona-aware first-run empty states**: pure `computeFirstRunSurfaceState()` behind a shared `FirstRunEmptyState` used by both POS grid and Inventory Products tab; never flashes a CTA while the catalogue is still streaming (invariant #11).
- **2026-07-04** — **#31 get-started checklist**: `computeGetStartedChecklist()` derives completion from data (products/orders/staff counts), never stored as flags; the only persisted bit is the device-local dismissal `get_started_checklist_dismissed_v1`; new `OrdersDao.watchAnyOrderExists()`.
- **2026-07-04** — **#30 Fast-Add Product form**: pure `resolveFastAdd()` model owns validation, business-type unit default, wholesaler-mirror-on-save, and the blank-buying Uncosted rule; every `FastAddInvalid.field` must name a visible field; the Receive Stock mini-form is untouched.
- **2026-07-04** — **migration 0131 configurable trial length**: `set_business_trial_end()` late-binds `console_get_trial_days()` with a defensive 30-day fallback (active until the console getter ships); no redeploy of 0131 needed when it does.
- **2026-07-04** — **Saved Carts ListTile ink fix (repo convention)**: wrap a `ListTile` in a transparent `Material` whenever an opaque `Container`/`DecoratedBox` sits between it and the nearest `Material`, else its background/ink splash is invisible.
- **2026-07-04** — **daos.dart split into 11 part files**: pure locality refactor — carved into `daos_*.dart` `part of 'daos.dart'`, still one library so private sentinels stay visible and generated `daos.g.dart` is unchanged (no build_runner rerun).
- **2026-07-04** — **Order module extraction (ADR 0004)**: `lib/shared/services/orders/` — `OrderService` facade over `OrderCommands` (Checkout/Confirm/Cancel) + `OrderQueries` + a `SaleFlusher` seam; Confirm now settles crate-return then flips pending→completed (settlement moved off the UI; `CrateReturnModal` only collects counts).
- **2026-07-03** — **#23 Business-Scoped Stream primitive**: `business_scoped_stream.dart` — `currentBusinessIdProvider` seam + `businessScopedStream`/Family/AutoDispose factories hand `(ref, db, businessId)` with a guaranteed non-null id; a ban test forbids raw `StreamProvider` in `lib/` (name-keyed allowlist).
- **2026-07-03** — **#22 gate enforcement flipped strict**: static-ban allowlist emptied, bare `hasPermission(ref,…)` and `isManagerOrAbove()` deleted from the app; the tier rule now lives ONLY in registry atoms; `Gates.all` = 48.
- **2026-07-03** — **#21 Settings & sidebar named gates**: `viewSyncIssues` = `sync.view OR CEO` composite retires the `canViewSyncIssues` helper; settings write re-checks use `.allowsNow` + `showGateDenied`; new `manageSettings`/`deleteBusiness` gates.
- **2026-07-03** — **#20 operations named gates**: new `Gate.tierIn(ranks)` atom (fails closed on null) for Daily Stock Count's {CEO,Manager,Stock keeper} set that skips Cashier; screen guards use `Guarded.screen` and MainLayout tabs keep chrome on loading/denied.
- **2026-07-03** — **#18 Dashboard/Reports composite gates**: messiest CEO-or-tier-with-key tiles lifted verbatim into named gates (`seeSalesMetric` etc.); `(isManager||isCashier)`→`tierAtLeast(cashier)` is provably neutral under the CEO disjunct.
- **2026-07-02** — **#17 named-gate registry tracer (ADR 0002)**: new `lib/core/permissions/` — pure `Gate` algebra (fails closed while role unresolved, CEO all-on), `Gates` registry, `Guarded`/`Guarded.screen`, `.allows`/`.allowsNow`/`.require`; `gate_static_ban_test` ratchets bare `hasPermission` sites toward zero.
- **2026-07-01** — **#15 SyncedTable registry**: one ordered `List<SyncedTable>` in `sync_registry.dart` (part of app_database.dart) is the complete per-table truth; the six scattered constructs now derive from it; golden-equivalence + membership tests enforce it.
- **2026-07-01** — **widen cloud money to bigint (0130)**: int4 caps money at ₦21.47M → 22003 overflow permanently jams the outbox; `0130_widen_money_columns_to_bigint.sql` widened all 30 `*_kobo` + 4 crate-count columns; code-standards now mandates bigint for cloud money.
- **2026-07-01** — **device registry cloud-only (0129)**: `public.devices` (make/model/OS/version/last-seen) written by direct fire-and-forget upsert, NOT sync-queued; rows survive business deletion (no FK); `last_seen_at` server-stamped by trigger.
- **2026-07-01** — **clearAllData cursor-survival trap**: the per-business pull cursor in SharedPreferences survived `clearAllData`, forcing an incremental pull that never re-downloaded catalogue/roles; new `SyncCursorResetService.clearAll()` clears all pull-state keys in `clearAllData`, and a one-shot `cursor_reset_v3` flag auto-heals stuck devices.

---

## June 2026

- **2026-06-30** — **outbox-is-sacred (Invariant #12)**: `SyncDao.pendingRowIds()` guards it — restore skips clobbering rows with pending local edits (LWW demoted to a tiebreaker), reconcile skips pending/deferred tables, logout push-confirms an empty outbox before wiping (else the Resolve-unsynced-data dialog), and `pushThenPull` uploads before downloading.
- **2026-06-30** — **First-Load overlay redesign**: post-login loader capped ≤2s (400ms floor / 2s cap) via `FirstLoadOverlayController` (sole source of truth) handing off to background sync + skeletons; first-load marker cleared in `clearAllData`; restore now wraps each table in one Drift transaction.
- **2026-06-27** — **auth screens width-capped on tablet/desktop**: all auth screens (Welcome, Sign-In, OTP, SignUp, Lock) constrain content to 480dp max-width on `!context.isPhone` viewports; background glows stay full-bleed.
- **2026-06-27** — **desktop/tablet responsive shell**: desktop MainLayout shows a persistent 280dp AppDrawer sidebar (no bottom nav), tablet a rail; product/receive grids subtract the 280dp sidebar width from screen-width calcs to avoid squished cards.
- **2026-06-27** — **Wallet → Credits Balance terminology (UI only)**: user-facing "Wallet"/"Top-up" renamed to "Credits Balance"/"Ledger Entries" (WalletService→CreditLedgerService, walletBalancesKoboProvider→creditBalancesKoboProvider), but DB tables (wallet_transactions, customer_wallets), RLS/RPCs, permission keys, and the `'wallet'` payment-type enum are kept unchanged for sync compat.
- **2026-06-26** — **post-OTP account-resolution spinner**: otp_verification_screen shows a `_resolving` scrim spinner during post-verify network work (fetchSupabaseAccount + syncOnLogin); the embedded CEO/staff sign-up OTP steps only `_goTo` locally, so they were left untouched.
- **2026-06-26** — **fresh-login loading overlay is non-blocking**: SyncPullBanner computes percent from tablesDone/tablesTotal and shows a centered spinner overlay wrapped in IgnorePointer during PullStage.background; it never gates MainLayout mount.
- **2026-06-26** — **permissions catalogue re-seeds on empty**: clearAllData wipes the static `permissions` catalogue (deliberately never pulled by sync), so `ensurePermissionsSeeded()` re-seeds when COUNT==0, called from beforeOpen + end of clearAllData.
- **2026-06-26** — **allStoresProvider must guard null business**: it watches `authProvider.select(businessId)` and returns empty while null instead of calling whereBusiness()/requireBusinessId() (which throws StateError on unbound business and poisons the provider for the whole session during the create-business null-businessId window).
- **2026-06-26** — **cross-device existing email via RPC (0128)**: detection uses `current_user_linked_business()` (migration 0128, SECURITY DEFINER, keyed on users.auth_user_id like enforcement), NOT profiles.business_id, routing cross-device accounts to ExistingAccountScreen right after OTP; no pre-OTP existence oracle (email-enumeration guard).
- **2026-06-26** — **snapshot the receipt payment label**: checkout receipt/thermal read a snapshotted `_receiptPaymentLabel` (captured at confirm), not the live `_paymentLabel` getter, because the success flow resets PayMode when clearing the customer; the Orders badge categorizer checks `wallet && cash`→Partial before plain wallet.
- **2026-06-25** — **sqlite3 build hook set to system source**: pubspec `hooks.user_defines.sqlite3.source: system` stops the sqlite3 3.x build hook from downloading libsqlite3 from github.com at release-build time; the app already ships the lib via sqlite3_flutter_libs (dlopen).
- **2026-06-25** — **pull-to-refresh: banner is the sole animation**: AppRefreshWrapper is a lean RefreshIndicator with transparent colors (gesture only) firing `pullChanges` unawaited; SyncPullBanner is the sole sync animation (the custom `_PullOrb` was removed — no double animation, no content drag-down); `AlwaysScrollableScrollPhysics` makes it fire on short/empty screens.
- **2026-06-25** — **expense_budgets added to pull snapshot (0127)**: migration 0127 CREATE OR REPLACE pos_pull_snapshot to include `'expense_budgets'` (it was in RLS/realtime/pull loops but missing from the snapshot RPC, so cold-start/reinstall/first-login peers never received the monthly budget); applied idempotently due to divergent remote history.
- **2026-06-25** — **RefreshIndicator consolidated app-wide**: every raw RefreshIndicator was converted to AppRefreshWrapper (orders, activity, staff, customer detail); pull-to-refresh added to Payments + Stores; switched from syncAll to pullChanges for a clean banner cycle.
- **2026-06-25** — **pre-products bootstrap cluster must be FK-resilient**: the whole pre-`products` bootstrap cluster in `_restoreTableData` (stores, roles, role_permissions, user_businesses, etc.) uses `_insertResilient` (skip-and-defer on FK-787); a plain insert there aborts the entire pull and blanks the app.
- **2026-06-24** — **auth_user_id removed from users push whitelist**: dropped from `_pushableColumns['users']` because it's cloud-RPC-set only (local always null) and the client push was clobbering the RPC-stamped uid to null (ExistingAccountScreen showed "Member"); already-nulled rows need a one-time backfill.
- **2026-06-24** — **Quick Sale modal AnimationController lazy-init**: a `late final` AnimationController with inline initializer fires during dispose() if never accessed, doing a TickerMode ancestor lookup on a deactivated element (crash) — make it nullable/lazy via a getter + `_pulse?.dispose()`; Quick Sale cart lines must store an int iconCodePoint + null colour (not raw IconData/Color) to stay JSON-serializable for held carts.
- **2026-06-24** — **supplier account is a wallet in reconciliation**: recon net-position line uses `supplierWalletBalanceKobo` (= −supplierPayableKobo) — negative = "Owed to suppliers" (red), positive = "Supplier credit held" (green) — matching the Supplier Accounts convention.
- **2026-06-24** — **no preseeded product categories**: removed the fresh-business default-category seed block from add_product_screen; categories are create-as-you-go via the searchable field only (no SQL/onboarding seed site exists).
- **2026-06-24** — **blocking sync loader fully removed (offline-first entry, Invariant #11)**: FirstSyncScreen + `_BackgroundPullLoading` gates deleted from `_HomeRouter`; a logged-in device falls straight to MainLayout (only a local-businesses-query `_BrandedSplash` remains), the 4 render-critical tables still pull inline at sign-in, and the full pull fires non-blocking. (The earlier "loader gates on `!hasLocalProducts && stage != completed`" regression — logged twice on 06-24 — is superseded here.)
- **2026-06-24** — **verify role-binding before entering the app (H1)**: `_commit` retries the post-onboarding pull up to 3× and verifies `hasLocalRoleBinding` (membership + roles row + ≥1 role_permissions grant) before handoff, else keeps the draft and resets to the PIN step; the draft is cleared only after binding is verified so the idempotent commit can safely re-run.
- **2026-06-24** — **receive checkout explicit store dropdown**: receive checkout replaced the read-only "Receiving for" with a "STOCKING INTO" AppDropdown from selectableStoresProvider (user-mutable, defaults lockedStore ?? firstSelectable); dropped the §15.7 active-store-change abort in `_confirm`.
- **2026-06-24** — **receive grid on-hand mirrors Inventory scope** (logged twice on 06-24): Receive Stock display count mirrors Inventory semantics — locked store → watchProductDatasWithStockByStore, All-Stores → watchAllProductDatasWithStock — not the first-selectable-store fallback that showed only one store's stock.
- **2026-06-23** — **never auto-default to All Stores**: the §12.1 active-store default runs for every user and silently defaults to selectableStores.first unless an all-stores viewer has latched `allStoresChosen` (navigation_service) via an explicit picker pick; "All Stores" is only ever a deliberate choice.
- **2026-06-23** — **Google sign-in cancel vs error split**: signInWithGoogle split-catches PlatformException — `sign_in_cancelled`→null (quiet), all else logs an auth.google_signin_error breadcrumb + throws; release DEVELOPER_ERROR (status 10) requires registering the signing-cert SHA-1 as an Android OAuth client (operator step, not code).
- **2026-06-23** — **native Google account picker**: replaced the Google OAuth browser/Custom-Tab redirect with the native ID-token flow (`google_sign_in` SDK → `supabase.auth.signInWithIdToken`); `googleWebClientId` lives in main.dart and is passed via authProvider, with the iOS reversed-client-id URL scheme in Info.plist.
- **2026-06-23** — **multi-device sessions (single-active-session disabled)**: switched to multiple concurrent active sessions per identity — `_kickOtherDevices` renamed `_registerCloudSession` with the session-revocation update and `signOut(scope: others)` removed; the single-active-session constraint is disabled in the architecture docs. (Contrast the 06-18 "session expired = kick" entry — this reverses it.)
- **2026-06-23** — **hasLocalProductsProvider + _HomeRouter extraction**: introduced `hasLocalProductsProvider` (self-healing StreamProvider<bool> over product stock) and extracted the routing IIFE into a `_HomeRouter` ConsumerWidget (ref.watch at top of build); the blocking products gate it added was later removed by the offline-first loader removal.
- **2026-06-23** — **email-first CEO onboarding**: "Create a new business" opens EmailEntryScreen with `createBusinessIntent: true` to verify the email up-front (verified-new → CeoSignUpScreen, existing → sign-in), catching conflicts before the wizard rather than at final commit.
- **2026-06-23** — **branded invite email + OTP sender (0126)**: Edge Function send-invite-email (Resend, gated by x-invite-hook-secret, deployed --no-verify-jwt); migration 0126 adds cloud-only invite_codes.invite_email_sent_at + AFTER-INSERT trigger trg_send_invite_email via pg_net (URL+secret from Vault), AFTER INSERT only so sync re-push upserts never re-send; OTP-from-Reebaplus still needs Custom SMTP (operator).
- **2026-06-23** — **delete_business captures auth-delete failure (0125)**: migration 0125 adds account_deletion_events.auth_delete_error and makes delete_business GET STACKED DIAGNOSTICS on the best-effort `DELETE FROM auth.users` (a SECURITY DEFINER postgres function can't reliably delete auth.users on managed Supabase — that's the Admin API); cascade otherwise identical to 0113.
- **2026-06-23** — **wipe staff device when its business is deleted**: AuthService.wipeIfActiveBusinessDeleted() checks the cloud tombstone (confirmBusinessDeleted) at cold start + on resume from WhoIsWorking/single-staff LoginScreen, wiping local data + full logout → WelcomeScreen when confirmed deleted.
- **2026-06-23** — **inline supplier create + optional manufacturer**: SupplierFormSheet.show returns Future<SupplierData?>; Add Product + Receive Checkout gain an inline "Add supplier" (gated on suppliers.manage) that auto-selects the created supplier; Manufacturer is required only when trackEmpties is effectively on (bottle + isCrateBusiness + trackEmpties).
- **2026-06-23** — **device-authenticated staff = pinHash + membership**: the Who's-working picker, "Login with PIN" side-door, and logout branching key on device-authenticated users (a `users` row with pinHash!=null + active membership; pinHash is local-only, never synced); sole-user logout with pending offline throws LogoutWipeException, multi-user logout clears only the leaver's PIN.
- **2026-06-23** — **business logo on receipts**: BusinessLogoService (pick/resize ≤512² PNG, local cache + Supabase Storage upsert, ensureCached download-once) renders logoPath on visual + thermal receipts; requires an operator-created public `business-logos` Storage bucket; also introduced core/result.dart `Result<T,E>`/`AppError`.
- **2026-06-23** — **custom-price floor at the discount cap**: cart setCustomPrice clamps to `floorKobo = catalogKobo*(100-maxPercent)/100` and re-clamps line discounts, so the effective unit price can never drop below the CEO-allotted max-discount floor.
- **2026-06-22** — **local pagination + first-sync RPC retirement**: history lists page locally from Drift via compound keyset cursors (ORDER BY created_at DESC, id DESC) with live-head + paged-tail notifiers and SQL-aggregate summaries; `shouldUseSnapshotRpc = !isSlow && since != null`, so full/first pulls (since==null) always use the paginated PostgREST path and pos_pull_snapshot is retained only for incremental-on-fast pulls.
- **2026-06-22** — **walk-in hidden on receipt**: receipt (visual + thermal) renders the customer block only when customerName is non-empty and not `'Walk-in Customer'` (case-insensitive); walk-in leaves the section blank.
- **2026-06-22** — **per-business crate-tracking opt-in (v57/0123)**: crate visibility decoupled from business type via `businessTracksCrates = isCrateBusiness(type) && tracksEmptyCrates`; schema v56→v57 adds Businesses.tracksEmptyCrates (default true), cloud 0123 adds the column + extends complete_onboarding (p_tracks_empty_crates), 0124 drops the stale 10-arg overload (PGRST203).
- **2026-06-22** — **product detail screen is view-only**: product_detail_screen is strictly view-only for all roles (Delete removed, bottom action replaced with a static "VIEW ONLY" notice). (An earlier same-day `if (false && _canEdit)` stopgap was superseded by this full conversion.)
- **2026-06-22** — **wallet-covered credit sale reclassified as wallet**: order_service.addOrder takes walletBalanceKobo → _resolvePaymentType classifies an unpaid sale fully covered by existing wallet credit as `'wallet'`; checkout blocks cash/transfer and steers to Wallet when a registered customer has positive credit.
- **2026-06-22** — **Request Stock is a dedicated screen**: request_stock_sheet deleted; RequestStockScreen (GlassyScaffold, prefix icons, mutual-exclusion store logic) opened via Navigator.push from store details.
- **2026-06-22** — **resetStuckInProgress dedup-collision fix**: SyncDao.resetStuckInProgress replaces the bulk syncing→pending flip with a 3-step txn (delete stuck syncing rows with an existing pending twin, delete all-but-newest among remaining stuck twins, then flip survivors) to avoid 2067 on the partial dedup index; enqueueUpsert never mutates an in-flight syncing row.
- **2026-06-22** — **GlassyCard bypasses BackdropFilter during transitions**: route-transition jank is raster-thread BackdropFilter re-rasterization, so the centralized GlassyCard (and OptimizedBackdropFilter for dropdowns/tabbars) uses ModalRoute transition animations to skip the blur while transitioning, rendering frosted glass only once settled; a custom SlideLeftPageTransitionsBuilder replaces Cupertino across all 8 themes.
- **2026-06-22** — **per-store transfer history**: StockTransferDao.watchHistoryForStore watches received/cancelled transfers involving a store in either direction, surfaced as a read-only "Transfer history" ExpansionTile (≤30) in store_transfer_hub.
- **2026-06-22** — **auto-lock chip legibility + off toggle**: auto-lock ChoiceChips get explicit theme colors (onSurface label) for light-theme legibility; re-tapping the selected chip saves `0` to auto_lock_interval_seconds to turn auto-lock off (AutoLockWrapper gates on >0).
- **2026-06-22** — **supplier Total In/Out tiles**: supplier history screens show Total In (payments, signedAmountKobo>=0) / Total Out (invoices, <0), both skipping voided entries and referenceType=='void'.
- **2026-06-22** — **store-scoped transfer UI (Units 4–5)**: transfers moved into a per-store store_transfer_hub (request→dispatch→receive); Stores entry/list is visible to any transfer-permission holder (Add/Edit hidden on stores.manage); retired stock_transfer_screen.dart + incoming_transfers_screen.dart.
- **2026-06-22** — **requester-initiated transfer DAO (Unit 3)**: StockTransferDao gains requestTransfer (pending header, no stock move), dispatchTransfer (guards pending, optional qty change, decrements source at dispatch → in_transit), rejectRequest (→ cancelled); reuses stock_transfers (pending was already in the CHECK constraint, no migration).
- **2026-06-22** — **stores.request/dispatch_transfer perms (0122)**: two new keys stores.request_transfer + stores.dispatch_transfer (default CEO+Manager), stores.manage narrows to store CRUD; catalogue 36→38, schema v55→v56 INSERT OR IGNORE onUpgrade, migration 0122 seeds catalogue + updates seed_default_roles_for_business + backfills grants (catalogue-before-grants FK order).
- **2026-06-22** — **empties collected per-manufacturer (receive)**: receive_stock_service.confirmReceipt param is emptiesReturnedByManufacturer (manufacturerId→qty), recording crate-return once per manufacturer in a post-loop pass (the manufacturer owns the deposit); stock increment stays per-line and atomic.
- **2026-06-22** — **long-press hint icon moved to product card**: the tap-and-hold educational info icon moved from POS/Receive app bars to a Stack top-right on each product card, gated on the relevant permission.
- **2026-06-22** — **four UI fixes**: active-store label as app-bar subtitle across screens (activeStoreLabelProvider); Staff Management wrapped in SharedScaffold with MenuButton; AppBarHeader `truncateTitleWithReveal` for long business names.
- **2026-06-22** — **hints self-dismiss after 2 views**: UiHintService tracks hint view counts in SharedPreferences and hides a hint after it's viewed twice; long-press haptics (mediumImpact) on grid long-press + cart taps; hint icons gated by the relevant permission (hide-don't-block).
- **2026-06-22** — **Add/Update product form: searchable category, no Size**: Category is a searchable AppInput with create-as-you-go (auto-resolves a typed-but-unselected value in `_save`, starts empty); the Size dropdown is removed from the UI while `_size` is preserved in the data layer; placeholders are crate-context-aware.
- **2026-06-21** — **POS access-denied flash fix**: `currentUserPermissionsReadyProvider` distinguishes "permissions loading" from "denied" (empty set is otherwise ambiguous), and pos_home_screen's sales.make full-screen gate waits for it before showing denial (neutral empty scaffold while unresolved); the same provider is available for the other full-screen denial gates.
- **2026-06-21** — **Record Damages controller must be State-owned**: the damage-quantity controller (`_damageQtyCtrl`) must be created once and disposed in `State.dispose()`, never created locally and disposed inline after the sheet closes, to avoid a dispose/teardown race crash.
- **2026-06-21** — **stored-empty damage is crate-only (§17.2)**: the `empty` fate (stored returned-empty damaged) never calls `adjustStock` — removes no bottle stock, books no drink cost, only `InventoryDao.recordEmptyCrateDamage`; deposit loss sums full-crate from `+cratelost` rows (`damageForfeitsFullCrate`) + stored-empty from `damaged` ledger rows (`watchAllCrateDamages`), no double-count.
- **2026-06-20** — **forfeited crate deposit on Statement (§17.2)**: damaging a crate-tracked bottle records the crate's fate (none/full/empty) as a plain-text adjustment-reason suffix (`damage:<key>+cratelost`/`+crateempty`, no migration); forfeited deposit = units × `Manufacturers.depositAmountKobo`, subtracted from net profit as "Crate deposit loss".
- **2026-06-20** — **Receive Stock route guard matches its FAB**: `receive_stock_screen.dart`'s guard must be `!stock.add && !products.add` (not products.add only) so a stock keeper can open the flow.
- **2026-06-20** — **non-sellers land on Home not POS**: MainLayout bounces a role lacking `sales.make` off the POS(1)/Cart(8) tabs to Home(0), gated on the permission set being resolved so CEO/Manager/Cashier is never wrongly redirected mid-load.
- **2026-06-20** — **cross-business invite/onboarding reject (0120/0121)**: migrations 0120 (`redeem_invite_code`) + 0121 (`complete_onboarding`) reject with typed P0001 when `auth_user_id` is already bound to another business (enforcing one-email-one-business before the global-unique `users_auth_user_id_key` collides); staff sign-up skips the cloud-hydrate fallback on that rejection.
- **2026-06-20** — **Receive Stock Phase 2 (routing + supplier payment)**: all restocking routes through Receive Stock via a `receiveMode` flag (default false preserves onboarding direct-write); per-line price edits gated (buying→`products.edit_buying_price`, retail/wholesale→`products.edit_price`) persist via `CatalogDao.updateProductPrices`; optional checkout supplier payment gated on `suppliers.manage`, recorded atomically via `SupplierAccountService.recordPayment`.
- **2026-06-20** — **Receive Stock Phase 1 atomicity**: `ReceiveStockService.confirmReceipt` runs invoice+stock+crate-return in one Drift txn; only the `returned` crate leg is valid (crate_ledger CHECK forbids `received`, so `recordCrateReceiveFromManufacturer` is dead); the dead inbound supplier-delivery flow (deliveries_screen/receive_delivery_sheet/delivery_service/model) was deleted, order-side DeliveryReceipt kept.
- **2026-06-19** — **checkout crate confirmation + theme-independent receipts**: the checkout crate section renders for any `_isCrateBusiness && crateLines.isNotEmpty` (editable deposit section, else read-only "Empty Crates Being Taken"); receipts stay theme-independent (fixed amber `0xFFF5A623`, never `colorScheme.primary`).
- **2026-06-19** — **Empty Crates on receipts**: receipt widget + ESC/POS builder render a dedicated Empty Crates section computed internally (`_computeEmpties`) from cart lines where `unit=='bottle' && trackEmpties`; history call-sites must plumb `unit`/`trackEmpties`/`manufacturerId` + manufacturer names.
- **2026-06-19** — **recon inventory-on-hand must be store-scoped**: `computeReconData` reads `productsWithStockProvider(lockedStore)` so inventory-on-hand is store-scoped like every other recon figure; `topItems` ranks all identifiable products and `grossMarginPct` is a `ReconData` getter.
- **2026-06-19** — **recon Statement split into flow/point-in-time**: `_statementCard` split into Net-result(flow) / Business-worth(point-in-time) / Other-context, using semantic colors (`AppSemanticColors.success`/`colorScheme.error`); crate deposit treated as a recoverable asset (+) in net position.
- **2026-06-19** — **recon sales/crates cards**: Sales summary shows top-3 `topItems`; `_cratesCard` lists per-manufacturer holds valued at each manufacturer's deposit and is gated to track-empties businesses.
- **2026-06-19** — **full crate count is bottle-only**: `watchFullCratesByManufacturer` must filter `unit=='bottle' && trackEmpties=true` (matching `createOrder`), so a manufacturer's PET/Can stock never inflates the "Full" crate count.
- **2026-06-19** — **glassy background must be opaque**: a non-null `gradient` makes BoxDecoration ignore `color` entirely; use `AppDecorations.glassyBackground(context)`, which composites every gradient stop opaque via `Color.alphaBlend`, to stop the screen beneath bleeding through.
- **2026-06-19** — **saved carts are store-tagged**: `saved_carts` gained nullable `store_id` (Drift v55, cloud 0119; null = All Stores/legacy); recall is confined to the active store, and `loadCart` switches to the cart's origin store before writing lines.
- **2026-06-19** — **pinned SliverPersistentHeader extent contract**: a pinned `_SliverTabBarDelegate` must declare min/maxExtent from an `extent` param and wrap its child in `SizedBox(height: extent)` (call sites pass `context.getRSize(60)`) so rendered childExtent == maxExtent — the framework requires layoutExtent <= paintExtent.
- **2026-06-19** — **slide routes must not FadeTransition**: `slideDownRoute`/`slideLeftRoute` use SlideTransition only — a FadeTransition over the still-opaque outgoing screen double-exposes opaque Glassy pages (ghost text); SmoothRoute (pure fade) stays auth/onboarding-only.
- **2026-06-19** — **cart + badges store-gated (§12.1)**: cart + active-customer storage re-keyed from `userId` to `"userId|storeId"` (empty = All Stores); CartService swaps buckets on `lockedStoreId` change; Orders badge switched to `ordersDao.watchPendingOrders()` (carries storeId) filtered to the active store.
- **2026-06-19** — **custom cart-line price + `sales.set_custom_price`**: new CEO-only-default permission (Drift v54 + cloud 0118); a cart line carries immutable `catalogPriceKobo` + nullable `customPriceKobo`, `setCustomPrice` overwrites the effective price then re-clamps discount, and checkout staleness detection skips custom-priced lines.
- **2026-06-19** — **AppDropdown orElse type crash**: `AppDropdown<T>.buildUI` must resolve the selected item with a plain `for` loop, not `firstWhere(...orElse:)` — when T is nullable but items are non-null-valued, the `orElse` closure's reified type fails Dart's runtime subtype check and throws.
- **2026-06-19** — **supplier empty-crate tracking (§3.13)**: dedicated `supplier_crate_ledger` (append-only; `received`+N = we owe) + `supplier_crate_balances` cache (Drift v53, cloud 0117, natural key business_id,supplier_id,manufacturer_id), never overloading crate_ledger; the deposit headline is COMPUTED as Σ(positive balance × manufacturer deposit); Receive Delivery records a supplier crate receipt per tracked line.
- **2026-06-18** — **session upsert churn: reuse session id**: `SessionsDao.createSession` reuses the existing active (non-revoked/non-expired) session for the same device_id+user_id (bumps expires_at, re-enqueues under the same id) so re-auths coalesce into one outbox row; `_kickOtherDevices` cloud session insert→upsert avoids 23505.
- **2026-06-18** — **Reports hub has no period bar**: the Reports hub is just the card menu (each inner report owns its own period filter); `ProfitReportScreen.initialPeriod` is now optional, and order cards tap through to CustomerDetailScreen (walk-ins fall through to the receipt).
- **2026-06-18** — **auth.* diagnostics need explicit tenant**: `ErrorLogDao.logError` takes optional `businessId`/`userId` (bid = businessId ?? currentBusinessId) so session-loss breadcrumbs enqueue durably at teardown after AuthService.value is nulled — otherwise the `if (bid != null) enqueueUpsert` guard keeps them local-only.
- **2026-06-18** — **Supplier Accounts §21 gaps closed**: added a pre-write confirmation gate (`confirmSupplierActivity`); CEO-only ledger void appends an opposite-sign compensating row carrying the original store_id (`voidEntry` returns bool, double-void is a no-op); new `SupplierAccountsReportScreen` gated on `suppliers.manage`; the §3.13 crates section is a display-only placeholder.
- **2026-06-18** — **"session expired" = single-active-device kick**: `_kickOtherDevices` uses `signOut(scope: SignOutScope.others)`, which always preserves the requesting session, so sign-in can never revoke the signing-in device — a release "session expired" is the policy revoking THIS device because the account signed in elsewhere. (Later reversed by the 06-23 multi-device-sessions entry.)
- **2026-06-18** — **cold-start sync warm-up**: the first push of a session pays a cold DNS+TLS+radio tax that trips the tight 5s fail-fast timeout; `_warmUpConnection` fires a throwaway `businesses select limit 1` before the first drain (once per session via `_didWarmUpThisSession`), and the first drain's per-chunk timeout is floored at 10s.
- **2026-06-18** — **Empty Crates tab per-store (§16.8.1)**: locked store confines Full (`watchFullCratesByManufacturer(storeId:)`) and Empty (`store_crate_balances`), All Stores shows business-wide; a manual customer crate return is credited to the store of the customer's most-recent crate-bearing order (`resolveStoreForCustomerManufacturer`), falling back to the active store.
- **2026-06-17** — **Crates tab Full: match by id not name**: the full/empty watch streams emit maps keyed by manufacturer ID, so the per-card lookup in `_buildCratesTab` must match by `mfr.id` (not `mfr.name`) or every card falls to orElse with Full=0.
- **2026-06-17** — **revenue recognized at checkout**: revenue is recognized at checkout (order written `pending`); Confirm (`markCompleted`) is ceremonial. Use canonical `orderCountsAsSale(status)` / `orderRevenueStatuses = {pending, completed}` at every revenue site — never filter on `status=='completed'`.
- **2026-06-17** — **crate-balance upserts must notify streams**: balance-cache upserts must route through `customInsert(..., updates: {table})`, not raw `customStatement`, or `watchCrateBalancesWithGroups` never re-runs; a no-deposit sale leaving a customer owing crates fires a `customer_crate_debt` CEO+Manager notification.
- **2026-06-17** — **abort push cycle on degraded link**: `pushPending` sets a cycle-level `linkDegraded` flag on a timeout/transient-network/5xx chunk failure (not FK-deferred/permanent) and breaks the parent→child group loop, so children don't 23503 FK-fail when their parent chunk never landed; the queue retries whole in priority order next tick.
- **2026-06-17** — **debt/credit is always business-wide**: a customer's wallet is single business-wide (not per-store), so dashboard debt/credit folds over ALL customers and is never filtered by the customer's home store; sales/inventory/expenses stay store-scoped.
- **2026-06-17** — **sync retry hardening (§6.8)**: `markFailed` clamps backoff to 5 min normal / 15 min FK-deferred; `autoRecoverDueOrphans` re-enqueues only allowlisted self-healing reasons (`fk_deferred_cap_reached*`, `*created_at is immutable*`) with a per-orphan cap of 3 via a device-local `auto_retry_count` column (Drift v52, local-only, no cloud migration); terminal reasons stay manual.
- **2026-06-17** — **Drift Variable import in stream_providers**: `stream_providers.dart` must `import 'package:drift/drift.dart' show Variable;` to use `Variable(...)` in a customSelect, and read nullable columns via `readNullable<…>`.
- **2026-06-17** — **realtime sync must restart on resume**: `startRealtimeSync` guards `if (_tableChannels.isNotEmpty) return;` and never re-subscribes, so an OS-suspended websocket stays dead on a physical device; `restartRealtimeSync` (drops stale channels + re-subscribes) is called on app-resume and connectivity recovery.
- **2026-06-17** — **store cards must key by store id**: a keyless `_StoreCard` in a ListView.builder recycles position-matched state and streams the wrong store's inventory after a name-ordered insert; pass `key: ValueKey(store.id)` + a `didUpdateWidget` re-subscribe.
- **2026-06-16** — **owner role protection via ownerId**: `Businesses` gained nullable `ownerId` (Drift v50; already in `_pushableColumns['businesses']`, auto-picked-up on pull), set to authUserId in `createNewOwner`/`completeOnboarding`; staff detail hides "Change role" and re-checks at the write boundary when the target's authUserId matches `business.ownerId`.
